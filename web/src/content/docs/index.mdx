---
title: Linked Open Knowledge Format
description: Write OKF markdown, get a queryable knowledge graph for free.
template: splash
hero:
  tagline: A semantic profile of Google's Open Knowledge Format — write OKF markdown, get a queryable knowledge graph for free.
  actions:
    - text: Get started
      link: /getting-started/
      icon: right-arrow
    - text: Explore the graph
      link: /graph/
      icon: external
      variant: minimal
    - text: View on GitHub
      link: https://github.com/nicholsn/lokf
      icon: github
      variant: minimal
---

import { Card, CardGrid, Aside } from '@astrojs/starlight/components';
import { Content as ConceptGlance } from '../../generated/concept-glance.md';

LOKF keeps OKF's markdown-plus-frontmatter authoring model but binds every
concept, field, and relationship to **schema.org, W3C DCAT, and W3C PROV-O**,
so a bundle of markdown files is *also* valid **JSON-LD** that expands
losslessly to **RDF**. The format is defined once in **LinkML**; the JSON-LD
context, JSON Schema, SHACL shapes, and OWL ontology are all generated from
that single source.

## A concept at a glance

Ordinary OKF markdown, where every key has a defined RDF meaning:

<ConceptGlance />

Attach the published `lokf.context.jsonld` and this expands to RDF triples
using `schema:`, `prov:`, `dcterms:`, and `lokf:` predicates — no separate file.

<CardGrid>
	<Card title="Convert" icon="seti:markdown">
		`lokf convert` projects a concept or a whole bundle to Turtle,
		N-Triples, JSON-LD, or RDF/XML.
	</Card>
	<Card title="Query" icon="magnifier">
		`lokf query` runs SPARQL over a bundle loaded into an in-process
		Oxigraph store.
	</Card>
	<Card title="Serve" icon="rocket">
		`lokf serve` publishes a local SPARQL endpoint and a live graph
		explorer.
	</Card>
	<Card title="Automate" icon="puzzle">
		An MCP server and bundled agent skills let agents drive the whole
		toolkit.
	</Card>
</CardGrid>

<Aside type="caution" title="Draft profile">
	LOKF v0.1 is a **draft profile** and is **not affiliated with or endorsed by
	Google**. "Open Knowledge Format" / "OKF" refer to the format published by
	Google Cloud
	([`GoogleCloudPlatform/knowledge-catalog`](https://github.com/GoogleCloudPlatform/knowledge-catalog));
	LOKF extends it under its open terms.
</Aside>
