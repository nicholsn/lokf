"""Linked Open Knowledge Format (LOKF) build tooling.

The format itself is defined in ``lokf.yaml`` (LinkML) at the repository
root; this package regenerates every downstream artifact from it.
"""

__version__ = "0.1.0"
