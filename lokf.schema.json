{
    "$defs": {
        "Agent": {
            "additionalProperties": false,
            "description": "A person or organization responsible for a concept.",
            "properties": {
                "email": {
                    "description": "Contact email address.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "id": {
                    "description": "The concept's stable IRI. By convention it is the bundle base IRI joined with the Concept ID (the file path within the bundle, minus the `.md` suffix). Becomes the JSON-LD @id / RDF subject.",
                    "type": "string"
                },
                "name": {
                    "description": "A name.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "type": {
                    "description": "The concept's type. OKF's single required field. In LOKF the value SHOULD name a LOKF class (e.g. Metric, Dataset, Table, Playbook, GlossaryTerm); it designates the JSON-LD @type / rdf:type. Consumers MUST tolerate unknown values by treating the concept as a generic lokf:Concept.",
                    "enum": [
                        "Agent"
                    ],
                    "type": "string"
                }
            },
            "required": [
                "type",
                "id"
            ],
            "title": "Agent",
            "type": "object"
        },
        "AttestedComputation": {
            "additionalProperties": false,
            "description": "A sanctioned, immutable recipe for computing a value (OKF v0.2 \u00a710), carried as its own concept type. The frontmatter is the contract: the agent may only bind the declared `parameters`, the `executor` runs the bound computation and returns a receipt, and the `attester` deterministically compares what actually ran against this recipe. The computation text lives in the body's `# Computation` fenced block or in the file named by `computation`. Semantically a prov:Plan with an attestation contract layered on.",
            "properties": {
                "about": {
                    "description": "The subject matter this concept is about.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "attester": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/Attester"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "The deterministic, non-LLM verdict check over a receipt."
                },
                "author": {
                    "description": "The agent(s) responsible for authoring the concept.",
                    "items": {
                        "anyOf": [
                            {
                                "$ref": "#/$defs/Person"
                            },
                            {
                                "$ref": "#/$defs/Organization"
                            }
                        ]
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "body": {
                    "description": "The markdown body of the concept document (everything after the frontmatter). Mapped to schema:text; carried as a field only in the JSON-LD / JSON serialization, not duplicated in the frontmatter.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "citations": {
                    "description": "External sources supporting claims in the body.",
                    "items": {
                        "$ref": "#/$defs/Citation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "computation": {
                    "description": "Optional path to a file holding the computation; absent means the body's `# Computation` fenced block IS the computation. Minted in lokf: schema:contentUrl is the nearest real property but is scoped to MediaObject bytes.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "created": {
                    "description": "ISO 8601 datetime the concept was created.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "definedBy": {
                    "description": "A resource that formally defines this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "dependsOn": {
                    "description": "A concept this concept depends on.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "derivedFrom": {
                    "description": "An entity this concept was derived from (provenance).",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "description": {
                    "description": "A single-sentence summary of the concept.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "executor": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/Executor"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "How the computation is run and what evidence a run must return."
                },
                "generated": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/Generation"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "How and when the current content was produced: `by` (REQUIRED actor) and `at` (ISO 8601 datetime). Supersedes the v0.1 `timestamp` field; consumers MAY fall back to `timestamp` when `generated` is absent."
                },
                "genre": {
                    "$ref": "#/$defs/DiataxisMode",
                    "description": "The Di\u00e1taxis documentation mode of the concept's markdown body (tutorial, how-to, reference, or explanation). An optional facet that is orthogonal to `type`: `type` says what the concept is *about*, `genre` says how its prose *serves the reader*. Consumers MUST tolerate absent or unknown values."
                },
                "hasPart": {
                    "description": "A concept that is a part of this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "id": {
                    "description": "The concept's stable IRI. By convention it is the bundle base IRI joined with the Concept ID (the file path within the bundle, minus the `.md` suffix). Becomes the JSON-LD @id / RDF subject.",
                    "type": "string"
                },
                "isPartOf": {
                    "description": "The target concept that this concept is a part of.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "license": {
                    "description": "The license under which the concept or bundle is offered.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "parameters": {
                    "description": "The typed, named holes the agent may fill \u2014 the ONLY surface the agent may bind. Binding semantics follow `runtime`. Minted in lokf, mirroring the existing `fields` -> lokf:field idiom: schema.org attaches PropertyValueSpecifications only through the Actions -input/-output annotation syntax, which is not a reusable property.",
                    "items": {
                        "$ref": "#/$defs/Parameter"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "references": {
                    "description": "A concept or resource this concept refers to.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relatedTo": {
                    "description": "A generic association to another concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relations": {
                    "description": "Typed relationships whose predicate is not covered by a named relation slot. Each entry is a reified Relation (predicate + target).",
                    "items": {
                        "$ref": "#/$defs/Relation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "resource": {
                    "description": "A URI that uniquely identifies the underlying real-world asset the concept describes (a table console URL, an API base URL, etc.). Absent for purely abstract concepts.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "runtime": {
                    "description": "REQUIRED on an Attested Computation. How to run the computation (bigquery | postgres | dbt | python | Looker | ...); defines what `parameters` mean and how the executor and attester interpret the contract.",
                    "type": "string"
                },
                "sameAs": {
                    "description": "An IRI asserting this concept is the same entity as another.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "source": {
                    "description": "A resource from which this concept is derived or sourced.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "sources": {
                    "description": "The materials this concept derives from, each carrying objective credibility signals. Markdown footnotes join to entries via `sources[].id`. Supersedes `citations` and the body `# Citations` convention.",
                    "items": {
                        "$ref": "#/$defs/Source"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "stale_after": {
                    "description": "Absolute date (YYYY-MM-DD) on or after which the concept is stale (stale when today >= stale_after). schema:expires is the exact concept \u2014 a planned future expiry; prov:invalidatedAtTime would falsely assert the entity WAS invalidated, and dcterms:valid names a validity period only ambiguously.",
                    "format": "date",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "status": {
                    "$ref": "#/$defs/ConceptStatus",
                    "description": "Lifecycle status of the concept: draft | stable | deprecated. Absent means stable (consumer-side default; absence is deliberately distinguishable from an explicit `status: stable`, so no ifabsent). Advisory, never access control."
                },
                "tags": {
                    "description": "Short cross-cutting keywords for categorization.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "timestamp": {
                    "description": "ISO 8601 datetime of the last meaningful change.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "title": {
                    "description": "Human-readable display name.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "type": {
                    "description": "The concept's type. OKF's single required field. In LOKF the value SHOULD name a LOKF class (e.g. Metric, Dataset, Table, Playbook, GlossaryTerm); it designates the JSON-LD @type / rdf:type. Consumers MUST tolerate unknown values by treating the concept as a generic lokf:Concept.",
                    "enum": [
                        "AttestedComputation"
                    ],
                    "type": "string"
                },
                "usage_window": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/UsageWindow"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "The `{ from, to }` date range framing every `usage_count`. Attached both to Concept (the shared window, sibling of `sources`) and to Source (a per-entry override)."
                },
                "verified": {
                    "description": "Verification events confirming the content against its sources or resource. Independent of `generated`: who wrote a concept need not be who confirmed it. Trust tiers derive from the actors: absent = unverified; only non-human actors = machine-confirmed; any `human:<id>` actor = human-reviewed. A bare `{ by, at }` mapping MUST be read as a one-element list; the LOKF loader normalizes the bare form before LinkML validation. Minted in lokf: no established vocabulary has an entity-to-verification-event property.",
                    "items": {
                        "$ref": "#/$defs/Verification"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "version": {
                    "description": "A version string for the concept's content.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "required": [
                "runtime",
                "id",
                "type"
            ],
            "title": "AttestedComputation",
            "type": "object"
        },
        "Attester": {
            "additionalProperties": false,
            "description": "The deterministic, non-LLM check for an Attested Computation (OKF v0.2 \u00a710.2): `resource` names code that takes a run's receipt and returns a verdict, run consumer-side. Honestly lokf-minted.",
            "properties": {
                "resource": {
                    "description": "Path or URL to the deterministic verdict code.",
                    "type": "string"
                }
            },
            "required": [
                "resource"
            ],
            "title": "Attester",
            "type": "object"
        },
        "Citation": {
            "additionalProperties": false,
            "description": "A reference to an external source supporting a claim in a concept's body. Attached via the schema:citation predicate.",
            "properties": {
                "description": {
                    "description": "A single-sentence summary of the concept.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "title": {
                    "description": "Human-readable display name.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "url": {
                    "description": "A URL.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "title": "Citation",
            "type": "object"
        },
        "Concept": {
            "additionalProperties": false,
            "description": "A single unit of knowledge within a bundle, represented as one markdown document. The abstract base of every LOKF type. Its own IRI (`id`) is the RDF subject; the markdown body and typed relations become triples about that subject.",
            "properties": {
                "about": {
                    "description": "The subject matter this concept is about.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "author": {
                    "description": "The agent(s) responsible for authoring the concept.",
                    "items": {
                        "anyOf": [
                            {
                                "$ref": "#/$defs/Person"
                            },
                            {
                                "$ref": "#/$defs/Organization"
                            }
                        ]
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "body": {
                    "description": "The markdown body of the concept document (everything after the frontmatter). Mapped to schema:text; carried as a field only in the JSON-LD / JSON serialization, not duplicated in the frontmatter.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "citations": {
                    "description": "External sources supporting claims in the body.",
                    "items": {
                        "$ref": "#/$defs/Citation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "created": {
                    "description": "ISO 8601 datetime the concept was created.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "definedBy": {
                    "description": "A resource that formally defines this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "dependsOn": {
                    "description": "A concept this concept depends on.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "derivedFrom": {
                    "description": "An entity this concept was derived from (provenance).",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "description": {
                    "description": "A single-sentence summary of the concept.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "generated": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/Generation"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "How and when the current content was produced: `by` (REQUIRED actor) and `at` (ISO 8601 datetime). Supersedes the v0.1 `timestamp` field; consumers MAY fall back to `timestamp` when `generated` is absent."
                },
                "genre": {
                    "$ref": "#/$defs/DiataxisMode",
                    "description": "The Di\u00e1taxis documentation mode of the concept's markdown body (tutorial, how-to, reference, or explanation). An optional facet that is orthogonal to `type`: `type` says what the concept is *about*, `genre` says how its prose *serves the reader*. Consumers MUST tolerate absent or unknown values."
                },
                "hasPart": {
                    "description": "A concept that is a part of this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "id": {
                    "description": "The concept's stable IRI. By convention it is the bundle base IRI joined with the Concept ID (the file path within the bundle, minus the `.md` suffix). Becomes the JSON-LD @id / RDF subject.",
                    "type": "string"
                },
                "isPartOf": {
                    "description": "The target concept that this concept is a part of.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "license": {
                    "description": "The license under which the concept or bundle is offered.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "references": {
                    "description": "A concept or resource this concept refers to.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relatedTo": {
                    "description": "A generic association to another concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relations": {
                    "description": "Typed relationships whose predicate is not covered by a named relation slot. Each entry is a reified Relation (predicate + target).",
                    "items": {
                        "$ref": "#/$defs/Relation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "resource": {
                    "description": "A URI that uniquely identifies the underlying real-world asset the concept describes (a table console URL, an API base URL, etc.). Absent for purely abstract concepts.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "sameAs": {
                    "description": "An IRI asserting this concept is the same entity as another.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "source": {
                    "description": "A resource from which this concept is derived or sourced.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "sources": {
                    "description": "The materials this concept derives from, each carrying objective credibility signals. Markdown footnotes join to entries via `sources[].id`. Supersedes `citations` and the body `# Citations` convention.",
                    "items": {
                        "$ref": "#/$defs/Source"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "stale_after": {
                    "description": "Absolute date (YYYY-MM-DD) on or after which the concept is stale (stale when today >= stale_after). schema:expires is the exact concept \u2014 a planned future expiry; prov:invalidatedAtTime would falsely assert the entity WAS invalidated, and dcterms:valid names a validity period only ambiguously.",
                    "format": "date",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "status": {
                    "$ref": "#/$defs/ConceptStatus",
                    "description": "Lifecycle status of the concept: draft | stable | deprecated. Absent means stable (consumer-side default; absence is deliberately distinguishable from an explicit `status: stable`, so no ifabsent). Advisory, never access control."
                },
                "tags": {
                    "description": "Short cross-cutting keywords for categorization.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "timestamp": {
                    "description": "ISO 8601 datetime of the last meaningful change.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "title": {
                    "description": "Human-readable display name.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "type": {
                    "description": "The concept's type. OKF's single required field. In LOKF the value SHOULD name a LOKF class (e.g. Metric, Dataset, Table, Playbook, GlossaryTerm); it designates the JSON-LD @type / rdf:type. Consumers MUST tolerate unknown values by treating the concept as a generic lokf:Concept.",
                    "enum": [
                        "Concept"
                    ],
                    "type": "string"
                },
                "usage_window": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/UsageWindow"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "The `{ from, to }` date range framing every `usage_count`. Attached both to Concept (the shared window, sibling of `sources`) and to Source (a per-entry override)."
                },
                "verified": {
                    "description": "Verification events confirming the content against its sources or resource. Independent of `generated`: who wrote a concept need not be who confirmed it. Trust tiers derive from the actors: absent = unverified; only non-human actors = machine-confirmed; any `human:<id>` actor = human-reviewed. A bare `{ by, at }` mapping MUST be read as a one-element list; the LOKF loader normalizes the bare form before LinkML validation. Minted in lokf: no established vocabulary has an entity-to-verification-event property.",
                    "items": {
                        "$ref": "#/$defs/Verification"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "version": {
                    "description": "A version string for the concept's content.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "required": [
                "id",
                "type"
            ],
            "title": "Concept",
            "type": "object"
        },
        "ConceptStatus": {
            "description": "Lifecycle status of a concept (OKF v0.2 \u00a75.4). Absent means stable. Each value maps to a real IRI in the ADMS status controlled vocabulary (http://purl.org/adms/status/, the vocabulary DCAT-AP mandates for adms:status). On the flat-context path the value serializes as a text literal \u2014 sanctioned usage of schema:creativeWorkStatus, which is deliberately text-valued; the IRIs surface on the LinkML RDF/OWL path.",
            "enum": [
                "draft",
                "stable",
                "deprecated"
            ],
            "title": "ConceptStatus",
            "type": "string"
        },
        "Dataset": {
            "additionalProperties": false,
            "description": "A collection of data, published or curated for access and reuse.",
            "properties": {
                "about": {
                    "description": "The subject matter this concept is about.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "author": {
                    "description": "The agent(s) responsible for authoring the concept.",
                    "items": {
                        "anyOf": [
                            {
                                "$ref": "#/$defs/Person"
                            },
                            {
                                "$ref": "#/$defs/Organization"
                            }
                        ]
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "body": {
                    "description": "The markdown body of the concept document (everything after the frontmatter). Mapped to schema:text; carried as a field only in the JSON-LD / JSON serialization, not duplicated in the frontmatter.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "citations": {
                    "description": "External sources supporting claims in the body.",
                    "items": {
                        "$ref": "#/$defs/Citation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "created": {
                    "description": "ISO 8601 datetime the concept was created.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "definedBy": {
                    "description": "A resource that formally defines this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "dependsOn": {
                    "description": "A concept this concept depends on.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "derivedFrom": {
                    "description": "An entity this concept was derived from (provenance).",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "description": {
                    "description": "A single-sentence summary of the concept.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "distribution": {
                    "description": "Accessible forms of the dataset.",
                    "items": {
                        "$ref": "#/$defs/Distribution"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "fields": {
                    "description": "The columns / properties that make up a dataset's schema.",
                    "items": {
                        "$ref": "#/$defs/Field"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "generated": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/Generation"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "How and when the current content was produced: `by` (REQUIRED actor) and `at` (ISO 8601 datetime). Supersedes the v0.1 `timestamp` field; consumers MAY fall back to `timestamp` when `generated` is absent."
                },
                "genre": {
                    "$ref": "#/$defs/DiataxisMode",
                    "description": "The Di\u00e1taxis documentation mode of the concept's markdown body (tutorial, how-to, reference, or explanation). An optional facet that is orthogonal to `type`: `type` says what the concept is *about*, `genre` says how its prose *serves the reader*. Consumers MUST tolerate absent or unknown values."
                },
                "hasPart": {
                    "description": "A concept that is a part of this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "id": {
                    "description": "The concept's stable IRI. By convention it is the bundle base IRI joined with the Concept ID (the file path within the bundle, minus the `.md` suffix). Becomes the JSON-LD @id / RDF subject.",
                    "type": "string"
                },
                "isPartOf": {
                    "description": "The target concept that this concept is a part of.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "license": {
                    "description": "The license under which the concept or bundle is offered.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "references": {
                    "description": "A concept or resource this concept refers to.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relatedTo": {
                    "description": "A generic association to another concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relations": {
                    "description": "Typed relationships whose predicate is not covered by a named relation slot. Each entry is a reified Relation (predicate + target).",
                    "items": {
                        "$ref": "#/$defs/Relation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "resource": {
                    "description": "A URI that uniquely identifies the underlying real-world asset the concept describes (a table console URL, an API base URL, etc.). Absent for purely abstract concepts.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "sameAs": {
                    "description": "An IRI asserting this concept is the same entity as another.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "source": {
                    "description": "A resource from which this concept is derived or sourced.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "sources": {
                    "description": "The materials this concept derives from, each carrying objective credibility signals. Markdown footnotes join to entries via `sources[].id`. Supersedes `citations` and the body `# Citations` convention.",
                    "items": {
                        "$ref": "#/$defs/Source"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "stale_after": {
                    "description": "Absolute date (YYYY-MM-DD) on or after which the concept is stale (stale when today >= stale_after). schema:expires is the exact concept \u2014 a planned future expiry; prov:invalidatedAtTime would falsely assert the entity WAS invalidated, and dcterms:valid names a validity period only ambiguously.",
                    "format": "date",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "status": {
                    "$ref": "#/$defs/ConceptStatus",
                    "description": "Lifecycle status of the concept: draft | stable | deprecated. Absent means stable (consumer-side default; absence is deliberately distinguishable from an explicit `status: stable`, so no ifabsent). Advisory, never access control."
                },
                "tags": {
                    "description": "Short cross-cutting keywords for categorization.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "timestamp": {
                    "description": "ISO 8601 datetime of the last meaningful change.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "title": {
                    "description": "Human-readable display name.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "type": {
                    "description": "The concept's type. OKF's single required field. In LOKF the value SHOULD name a LOKF class (e.g. Metric, Dataset, Table, Playbook, GlossaryTerm); it designates the JSON-LD @type / rdf:type. Consumers MUST tolerate unknown values by treating the concept as a generic lokf:Concept.",
                    "enum": [
                        "Dataset"
                    ],
                    "type": "string"
                },
                "usage_window": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/UsageWindow"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "The `{ from, to }` date range framing every `usage_count`. Attached both to Concept (the shared window, sibling of `sources`) and to Source (a per-entry override)."
                },
                "verified": {
                    "description": "Verification events confirming the content against its sources or resource. Independent of `generated`: who wrote a concept need not be who confirmed it. Trust tiers derive from the actors: absent = unverified; only non-human actors = machine-confirmed; any `human:<id>` actor = human-reviewed. A bare `{ by, at }` mapping MUST be read as a one-element list; the LOKF loader normalizes the bare form before LinkML validation. Minted in lokf: no established vocabulary has an entity-to-verification-event property.",
                    "items": {
                        "$ref": "#/$defs/Verification"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "version": {
                    "description": "A version string for the concept's content.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "required": [
                "id",
                "type"
            ],
            "title": "Dataset",
            "type": "object"
        },
        "DiataxisMode": {
            "description": "The Di\u00e1taxis documentation mode of a concept's body - an orthogonal facet describing *how* the prose serves the reader, distinct from the concept's `type` (what it is *about*). Each value maps to a schema.org term. Optional: an absent value simply means the mode is unclassified, and consumers MUST treat an unknown value the same way rather than fail.",
            "enum": [
                "tutorial",
                "how-to",
                "reference",
                "explanation"
            ],
            "title": "DiataxisMode",
            "type": "string"
        },
        "Distribution": {
            "additionalProperties": false,
            "description": "A specific accessible form of a Dataset (a file, feed, or endpoint). Maps to dcat:Distribution.",
            "properties": {
                "access_url": {
                    "description": "A URL that gives access to a distribution.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "description": {
                    "description": "A single-sentence summary of the concept.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "media_type": {
                    "description": "The media (MIME) type of a distribution.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "name": {
                    "description": "A name.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "title": "Distribution",
            "type": "object"
        },
        "Document": {
            "additionalProperties": false,
            "description": "A general knowledge document that does not fit a more specific type.",
            "properties": {
                "about": {
                    "description": "The subject matter this concept is about.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "author": {
                    "description": "The agent(s) responsible for authoring the concept.",
                    "items": {
                        "anyOf": [
                            {
                                "$ref": "#/$defs/Person"
                            },
                            {
                                "$ref": "#/$defs/Organization"
                            }
                        ]
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "body": {
                    "description": "The markdown body of the concept document (everything after the frontmatter). Mapped to schema:text; carried as a field only in the JSON-LD / JSON serialization, not duplicated in the frontmatter.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "citations": {
                    "description": "External sources supporting claims in the body.",
                    "items": {
                        "$ref": "#/$defs/Citation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "created": {
                    "description": "ISO 8601 datetime the concept was created.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "definedBy": {
                    "description": "A resource that formally defines this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "dependsOn": {
                    "description": "A concept this concept depends on.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "derivedFrom": {
                    "description": "An entity this concept was derived from (provenance).",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "description": {
                    "description": "A single-sentence summary of the concept.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "generated": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/Generation"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "How and when the current content was produced: `by` (REQUIRED actor) and `at` (ISO 8601 datetime). Supersedes the v0.1 `timestamp` field; consumers MAY fall back to `timestamp` when `generated` is absent."
                },
                "genre": {
                    "$ref": "#/$defs/DiataxisMode",
                    "description": "The Di\u00e1taxis documentation mode of the concept's markdown body (tutorial, how-to, reference, or explanation). An optional facet that is orthogonal to `type`: `type` says what the concept is *about*, `genre` says how its prose *serves the reader*. Consumers MUST tolerate absent or unknown values."
                },
                "hasPart": {
                    "description": "A concept that is a part of this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "id": {
                    "description": "The concept's stable IRI. By convention it is the bundle base IRI joined with the Concept ID (the file path within the bundle, minus the `.md` suffix). Becomes the JSON-LD @id / RDF subject.",
                    "type": "string"
                },
                "isPartOf": {
                    "description": "The target concept that this concept is a part of.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "license": {
                    "description": "The license under which the concept or bundle is offered.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "references": {
                    "description": "A concept or resource this concept refers to.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relatedTo": {
                    "description": "A generic association to another concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relations": {
                    "description": "Typed relationships whose predicate is not covered by a named relation slot. Each entry is a reified Relation (predicate + target).",
                    "items": {
                        "$ref": "#/$defs/Relation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "resource": {
                    "description": "A URI that uniquely identifies the underlying real-world asset the concept describes (a table console URL, an API base URL, etc.). Absent for purely abstract concepts.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "sameAs": {
                    "description": "An IRI asserting this concept is the same entity as another.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "source": {
                    "description": "A resource from which this concept is derived or sourced.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "sources": {
                    "description": "The materials this concept derives from, each carrying objective credibility signals. Markdown footnotes join to entries via `sources[].id`. Supersedes `citations` and the body `# Citations` convention.",
                    "items": {
                        "$ref": "#/$defs/Source"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "stale_after": {
                    "description": "Absolute date (YYYY-MM-DD) on or after which the concept is stale (stale when today >= stale_after). schema:expires is the exact concept \u2014 a planned future expiry; prov:invalidatedAtTime would falsely assert the entity WAS invalidated, and dcterms:valid names a validity period only ambiguously.",
                    "format": "date",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "status": {
                    "$ref": "#/$defs/ConceptStatus",
                    "description": "Lifecycle status of the concept: draft | stable | deprecated. Absent means stable (consumer-side default; absence is deliberately distinguishable from an explicit `status: stable`, so no ifabsent). Advisory, never access control."
                },
                "tags": {
                    "description": "Short cross-cutting keywords for categorization.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "timestamp": {
                    "description": "ISO 8601 datetime of the last meaningful change.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "title": {
                    "description": "Human-readable display name.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "type": {
                    "description": "The concept's type. OKF's single required field. In LOKF the value SHOULD name a LOKF class (e.g. Metric, Dataset, Table, Playbook, GlossaryTerm); it designates the JSON-LD @type / rdf:type. Consumers MUST tolerate unknown values by treating the concept as a generic lokf:Concept.",
                    "enum": [
                        "Document"
                    ],
                    "type": "string"
                },
                "usage_window": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/UsageWindow"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "The `{ from, to }` date range framing every `usage_count`. Attached both to Concept (the shared window, sibling of `sources`) and to Source (a per-entry override)."
                },
                "verified": {
                    "description": "Verification events confirming the content against its sources or resource. Independent of `generated`: who wrote a concept need not be who confirmed it. Trust tiers derive from the actors: absent = unverified; only non-human actors = machine-confirmed; any `human:<id>` actor = human-reviewed. A bare `{ by, at }` mapping MUST be read as a one-element list; the LOKF loader normalizes the bare form before LinkML validation. Minted in lokf: no established vocabulary has an entity-to-verification-event property.",
                    "items": {
                        "$ref": "#/$defs/Verification"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "version": {
                    "description": "A version string for the concept's content.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "required": [
                "id",
                "type"
            ],
            "title": "Document",
            "type": "object"
        },
        "Executor": {
            "additionalProperties": false,
            "description": "How an Attested Computation is run (OKF v0.2 \u00a710.2): `resource` names run instructions or code a runner follows; `receipt` declares the field names a run must return \u2014 the evidence surface the attester inspects. No established vocabulary has an execution-interface class, so the IRI is honestly lokf-minted; schema:EntryPoint (an invocation specification) is the nearest real term.",
            "properties": {
                "receipt": {
                    "description": "The field names a run must return (e.g. job_id, executed_sql, result) \u2014 the evidence the attester inspects. Receipts themselves are runtime artifacts, never stored in the bundle. Minted in lokf: no established vocabulary describes a receipt-shape declaration.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "resource": {
                    "description": "Path or URL to the run instructions or code.",
                    "type": "string"
                }
            },
            "required": [
                "resource"
            ],
            "title": "Executor",
            "type": "object"
        },
        "Explanation": {
            "additionalProperties": false,
            "description": "An understanding-oriented, discursive treatment of a topic that permits reflection. The Di\u00e1taxis explanation mode as a first-class concept type; typically links to its subject matter with `about`.",
            "properties": {
                "about": {
                    "description": "The subject matter this concept is about.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "author": {
                    "description": "The agent(s) responsible for authoring the concept.",
                    "items": {
                        "anyOf": [
                            {
                                "$ref": "#/$defs/Person"
                            },
                            {
                                "$ref": "#/$defs/Organization"
                            }
                        ]
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "body": {
                    "description": "The markdown body of the concept document (everything after the frontmatter). Mapped to schema:text; carried as a field only in the JSON-LD / JSON serialization, not duplicated in the frontmatter.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "citations": {
                    "description": "External sources supporting claims in the body.",
                    "items": {
                        "$ref": "#/$defs/Citation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "created": {
                    "description": "ISO 8601 datetime the concept was created.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "definedBy": {
                    "description": "A resource that formally defines this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "dependsOn": {
                    "description": "A concept this concept depends on.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "derivedFrom": {
                    "description": "An entity this concept was derived from (provenance).",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "description": {
                    "description": "A single-sentence summary of the concept.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "generated": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/Generation"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "How and when the current content was produced: `by` (REQUIRED actor) and `at` (ISO 8601 datetime). Supersedes the v0.1 `timestamp` field; consumers MAY fall back to `timestamp` when `generated` is absent."
                },
                "genre": {
                    "$ref": "#/$defs/DiataxisMode",
                    "description": "The Di\u00e1taxis documentation mode of the concept's markdown body (tutorial, how-to, reference, or explanation). An optional facet that is orthogonal to `type`: `type` says what the concept is *about*, `genre` says how its prose *serves the reader*. Consumers MUST tolerate absent or unknown values."
                },
                "hasPart": {
                    "description": "A concept that is a part of this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "id": {
                    "description": "The concept's stable IRI. By convention it is the bundle base IRI joined with the Concept ID (the file path within the bundle, minus the `.md` suffix). Becomes the JSON-LD @id / RDF subject.",
                    "type": "string"
                },
                "isPartOf": {
                    "description": "The target concept that this concept is a part of.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "license": {
                    "description": "The license under which the concept or bundle is offered.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "references": {
                    "description": "A concept or resource this concept refers to.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relatedTo": {
                    "description": "A generic association to another concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relations": {
                    "description": "Typed relationships whose predicate is not covered by a named relation slot. Each entry is a reified Relation (predicate + target).",
                    "items": {
                        "$ref": "#/$defs/Relation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "resource": {
                    "description": "A URI that uniquely identifies the underlying real-world asset the concept describes (a table console URL, an API base URL, etc.). Absent for purely abstract concepts.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "sameAs": {
                    "description": "An IRI asserting this concept is the same entity as another.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "source": {
                    "description": "A resource from which this concept is derived or sourced.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "sources": {
                    "description": "The materials this concept derives from, each carrying objective credibility signals. Markdown footnotes join to entries via `sources[].id`. Supersedes `citations` and the body `# Citations` convention.",
                    "items": {
                        "$ref": "#/$defs/Source"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "stale_after": {
                    "description": "Absolute date (YYYY-MM-DD) on or after which the concept is stale (stale when today >= stale_after). schema:expires is the exact concept \u2014 a planned future expiry; prov:invalidatedAtTime would falsely assert the entity WAS invalidated, and dcterms:valid names a validity period only ambiguously.",
                    "format": "date",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "status": {
                    "$ref": "#/$defs/ConceptStatus",
                    "description": "Lifecycle status of the concept: draft | stable | deprecated. Absent means stable (consumer-side default; absence is deliberately distinguishable from an explicit `status: stable`, so no ifabsent). Advisory, never access control."
                },
                "tags": {
                    "description": "Short cross-cutting keywords for categorization.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "timestamp": {
                    "description": "ISO 8601 datetime of the last meaningful change.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "title": {
                    "description": "Human-readable display name.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "type": {
                    "description": "The concept's type. OKF's single required field. In LOKF the value SHOULD name a LOKF class (e.g. Metric, Dataset, Table, Playbook, GlossaryTerm); it designates the JSON-LD @type / rdf:type. Consumers MUST tolerate unknown values by treating the concept as a generic lokf:Concept.",
                    "enum": [
                        "Explanation"
                    ],
                    "type": "string"
                },
                "usage_window": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/UsageWindow"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "The `{ from, to }` date range framing every `usage_count`. Attached both to Concept (the shared window, sibling of `sources`) and to Source (a per-entry override)."
                },
                "verified": {
                    "description": "Verification events confirming the content against its sources or resource. Independent of `generated`: who wrote a concept need not be who confirmed it. Trust tiers derive from the actors: absent = unverified; only non-human actors = machine-confirmed; any `human:<id>` actor = human-reviewed. A bare `{ by, at }` mapping MUST be read as a one-element list; the LOKF loader normalizes the bare form before LinkML validation. Minted in lokf: no established vocabulary has an entity-to-verification-event property.",
                    "items": {
                        "$ref": "#/$defs/Verification"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "version": {
                    "description": "A version string for the concept's content.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "required": [
                "id",
                "type"
            ],
            "title": "Explanation",
            "type": "object"
        },
        "Field": {
            "additionalProperties": false,
            "description": "A single column / property within a Table or Dataset schema. Maps to schema:PropertyValue.",
            "properties": {
                "constraints": {
                    "description": "Free-text or expression describing constraints on a field.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "datatype": {
                    "$ref": "#/$defs/FieldType",
                    "description": "The data type of a field."
                },
                "description": {
                    "description": "A single-sentence summary of the concept.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "is_key": {
                    "description": "Whether the field is (part of) the primary key.",
                    "type": [
                        "boolean",
                        "null"
                    ]
                },
                "name": {
                    "description": "A name.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "unit": {
                    "description": "The unit of measurement (e.g. USD, seconds, count).",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "title": "Field",
            "type": "object"
        },
        "FieldType": {
            "description": "Datatypes for dataset/table fields, each mapped to its XSD (or RDF) type.",
            "enum": [
                "string",
                "integer",
                "number",
                "boolean",
                "date",
                "datetime",
                "time",
                "uri",
                "json"
            ],
            "title": "FieldType",
            "type": "string"
        },
        "Generation": {
            "additionalProperties": false,
            "description": "How and when the current content was produced (OKF v0.2 `generated`, \u00a75.2). Modeled as the PROV activity that generated the concept, so \"who wrote this, when\" is a one-hop join from the concept via prov:wasGeneratedBy, with both slots keeping PROV's activity-side semantics (prov:wasAssociatedWith, prov:endedAtTime) \u2014 domain/range correct with no reasoner or subproperty entailment required.",
            "properties": {
                "at": {
                    "description": "ISO 8601 datetime at which the event completed.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "by": {
                    "description": "The actor that performed the event, as a plain OKF \u00a77 actor string (`<producer>/<version>`, `human:<id>`, `process:<id>`) \u2014 a literal, never an IRI. Trust tiers derive from the `human:` prefix via string inspection (SPARQL STRSTARTS).",
                    "type": "string"
                }
            },
            "required": [
                "by"
            ],
            "title": "Generation",
            "type": "object"
        },
        "GlossaryTerm": {
            "additionalProperties": false,
            "description": "A defined term in a controlled vocabulary or glossary. Maps to schema:DefinedTerm and skos:Concept.",
            "properties": {
                "abbreviation": {
                    "description": "A short form or acronym for a term.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "about": {
                    "description": "The subject matter this concept is about.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "author": {
                    "description": "The agent(s) responsible for authoring the concept.",
                    "items": {
                        "anyOf": [
                            {
                                "$ref": "#/$defs/Person"
                            },
                            {
                                "$ref": "#/$defs/Organization"
                            }
                        ]
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "body": {
                    "description": "The markdown body of the concept document (everything after the frontmatter). Mapped to schema:text; carried as a field only in the JSON-LD / JSON serialization, not duplicated in the frontmatter.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "citations": {
                    "description": "External sources supporting claims in the body.",
                    "items": {
                        "$ref": "#/$defs/Citation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "created": {
                    "description": "ISO 8601 datetime the concept was created.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "definedBy": {
                    "description": "A resource that formally defines this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "definition": {
                    "description": "The formal definition of a term.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "dependsOn": {
                    "description": "A concept this concept depends on.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "derivedFrom": {
                    "description": "An entity this concept was derived from (provenance).",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "description": {
                    "description": "A single-sentence summary of the concept.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "generated": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/Generation"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "How and when the current content was produced: `by` (REQUIRED actor) and `at` (ISO 8601 datetime). Supersedes the v0.1 `timestamp` field; consumers MAY fall back to `timestamp` when `generated` is absent."
                },
                "genre": {
                    "$ref": "#/$defs/DiataxisMode",
                    "description": "The Di\u00e1taxis documentation mode of the concept's markdown body (tutorial, how-to, reference, or explanation). An optional facet that is orthogonal to `type`: `type` says what the concept is *about*, `genre` says how its prose *serves the reader*. Consumers MUST tolerate absent or unknown values."
                },
                "hasPart": {
                    "description": "A concept that is a part of this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "id": {
                    "description": "The concept's stable IRI. By convention it is the bundle base IRI joined with the Concept ID (the file path within the bundle, minus the `.md` suffix). Becomes the JSON-LD @id / RDF subject.",
                    "type": "string"
                },
                "isPartOf": {
                    "description": "The target concept that this concept is a part of.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "license": {
                    "description": "The license under which the concept or bundle is offered.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "references": {
                    "description": "A concept or resource this concept refers to.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relatedTo": {
                    "description": "A generic association to another concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relations": {
                    "description": "Typed relationships whose predicate is not covered by a named relation slot. Each entry is a reified Relation (predicate + target).",
                    "items": {
                        "$ref": "#/$defs/Relation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "resource": {
                    "description": "A URI that uniquely identifies the underlying real-world asset the concept describes (a table console URL, an API base URL, etc.). Absent for purely abstract concepts.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "sameAs": {
                    "description": "An IRI asserting this concept is the same entity as another.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "source": {
                    "description": "A resource from which this concept is derived or sourced.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "sources": {
                    "description": "The materials this concept derives from, each carrying objective credibility signals. Markdown footnotes join to entries via `sources[].id`. Supersedes `citations` and the body `# Citations` convention.",
                    "items": {
                        "$ref": "#/$defs/Source"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "stale_after": {
                    "description": "Absolute date (YYYY-MM-DD) on or after which the concept is stale (stale when today >= stale_after). schema:expires is the exact concept \u2014 a planned future expiry; prov:invalidatedAtTime would falsely assert the entity WAS invalidated, and dcterms:valid names a validity period only ambiguously.",
                    "format": "date",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "status": {
                    "$ref": "#/$defs/ConceptStatus",
                    "description": "Lifecycle status of the concept: draft | stable | deprecated. Absent means stable (consumer-side default; absence is deliberately distinguishable from an explicit `status: stable`, so no ifabsent). Advisory, never access control."
                },
                "tags": {
                    "description": "Short cross-cutting keywords for categorization.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "timestamp": {
                    "description": "ISO 8601 datetime of the last meaningful change.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "title": {
                    "description": "Human-readable display name.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "type": {
                    "description": "The concept's type. OKF's single required field. In LOKF the value SHOULD name a LOKF class (e.g. Metric, Dataset, Table, Playbook, GlossaryTerm); it designates the JSON-LD @type / rdf:type. Consumers MUST tolerate unknown values by treating the concept as a generic lokf:Concept.",
                    "enum": [
                        "GlossaryTerm"
                    ],
                    "type": "string"
                },
                "usage_window": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/UsageWindow"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "The `{ from, to }` date range framing every `usage_count`. Attached both to Concept (the shared window, sibling of `sources`) and to Source (a per-entry override)."
                },
                "verified": {
                    "description": "Verification events confirming the content against its sources or resource. Independent of `generated`: who wrote a concept need not be who confirmed it. Trust tiers derive from the actors: absent = unverified; only non-human actors = machine-confirmed; any `human:<id>` actor = human-reviewed. A bare `{ by, at }` mapping MUST be read as a one-element list; the LOKF loader normalizes the bare form before LinkML validation. Minted in lokf: no established vocabulary has an entity-to-verification-event property.",
                    "items": {
                        "$ref": "#/$defs/Verification"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "version": {
                    "description": "A version string for the concept's content.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "required": [
                "id",
                "type"
            ],
            "title": "GlossaryTerm",
            "type": "object"
        },
        "KnowledgeBundle": {
            "additionalProperties": false,
            "description": "A self-contained, hierarchical collection of concept documents \u2014 the unit of distribution (a git repo, tarball, or subdirectory). Maps to a DCAT Catalog. When a whole bundle is serialized as a single JSON-LD document, this class is the tree root; individual concept files validate against Concept (or a subclass).",
            "properties": {
                "base_iri": {
                    "description": "The base IRI against which Concept IDs are resolved to produce each concept's stable `id`.",
                    "format": "uri",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "concepts": {
                    "description": "The concepts contained in a bundle.",
                    "items": {
                        "anyOf": [
                            {
                                "$ref": "#/$defs/Dataset"
                            },
                            {
                                "$ref": "#/$defs/Metric"
                            },
                            {
                                "$ref": "#/$defs/Service"
                            },
                            {
                                "$ref": "#/$defs/Playbook"
                            },
                            {
                                "$ref": "#/$defs/Tutorial"
                            },
                            {
                                "$ref": "#/$defs/Explanation"
                            },
                            {
                                "$ref": "#/$defs/Policy"
                            },
                            {
                                "$ref": "#/$defs/GlossaryTerm"
                            },
                            {
                                "$ref": "#/$defs/Reference"
                            },
                            {
                                "$ref": "#/$defs/Document"
                            },
                            {
                                "$ref": "#/$defs/Role"
                            },
                            {
                                "$ref": "#/$defs/AttestedComputation"
                            },
                            {
                                "$ref": "#/$defs/Person"
                            },
                            {
                                "$ref": "#/$defs/Organization"
                            },
                            {
                                "$ref": "#/$defs/Table"
                            }
                        ]
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "context": {
                    "description": "The URL of the JSON-LD @context to attach to this bundle's concepts to interpret their frontmatter as Linked Data.",
                    "format": "uri",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "description": {
                    "description": "A single-sentence summary of the concept.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "license": {
                    "description": "The license under which the concept or bundle is offered.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "lokf_version": {
                    "description": "The LOKF version the bundle targets (e.g. \"0.1\").",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "okf_version": {
                    "description": "The OKF version the bundle remains compatible with (e.g. \"0.1\").",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "publisher": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/Person"
                        },
                        {
                            "$ref": "#/$defs/Organization"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "The agent responsible for making the bundle available."
                },
                "title": {
                    "description": "Human-readable display name.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "title": "KnowledgeBundle",
            "type": "object"
        },
        "Metric": {
            "additionalProperties": false,
            "description": "A precisely defined, measurable quantity \u2014 the canonical definition of a business or operational metric.",
            "properties": {
                "about": {
                    "description": "The subject matter this concept is about.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "author": {
                    "description": "The agent(s) responsible for authoring the concept.",
                    "items": {
                        "anyOf": [
                            {
                                "$ref": "#/$defs/Person"
                            },
                            {
                                "$ref": "#/$defs/Organization"
                            }
                        ]
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "body": {
                    "description": "The markdown body of the concept document (everything after the frontmatter). Mapped to schema:text; carried as a field only in the JSON-LD / JSON serialization, not duplicated in the frontmatter.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "citations": {
                    "description": "External sources supporting claims in the body.",
                    "items": {
                        "$ref": "#/$defs/Citation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "created": {
                    "description": "ISO 8601 datetime the concept was created.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "definedBy": {
                    "description": "A resource that formally defines this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "dependsOn": {
                    "description": "A concept this concept depends on.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "derivedFrom": {
                    "description": "An entity this concept was derived from (provenance).",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "description": {
                    "description": "A single-sentence summary of the concept.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "formula": {
                    "description": "The calculation or definition expression for a metric.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "generated": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/Generation"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "How and when the current content was produced: `by` (REQUIRED actor) and `at` (ISO 8601 datetime). Supersedes the v0.1 `timestamp` field; consumers MAY fall back to `timestamp` when `generated` is absent."
                },
                "genre": {
                    "$ref": "#/$defs/DiataxisMode",
                    "description": "The Di\u00e1taxis documentation mode of the concept's markdown body (tutorial, how-to, reference, or explanation). An optional facet that is orthogonal to `type`: `type` says what the concept is *about*, `genre` says how its prose *serves the reader*. Consumers MUST tolerate absent or unknown values."
                },
                "hasPart": {
                    "description": "A concept that is a part of this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "id": {
                    "description": "The concept's stable IRI. By convention it is the bundle base IRI joined with the Concept ID (the file path within the bundle, minus the `.md` suffix). Becomes the JSON-LD @id / RDF subject.",
                    "type": "string"
                },
                "isPartOf": {
                    "description": "The target concept that this concept is a part of.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "license": {
                    "description": "The license under which the concept or bundle is offered.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "measures": {
                    "description": "What the metric measures (a concept IRI or description).",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "references": {
                    "description": "A concept or resource this concept refers to.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relatedTo": {
                    "description": "A generic association to another concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relations": {
                    "description": "Typed relationships whose predicate is not covered by a named relation slot. Each entry is a reified Relation (predicate + target).",
                    "items": {
                        "$ref": "#/$defs/Relation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "resource": {
                    "description": "A URI that uniquely identifies the underlying real-world asset the concept describes (a table console URL, an API base URL, etc.). Absent for purely abstract concepts.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "sameAs": {
                    "description": "An IRI asserting this concept is the same entity as another.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "source": {
                    "description": "A resource from which this concept is derived or sourced.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "sources": {
                    "description": "The materials this concept derives from, each carrying objective credibility signals. Markdown footnotes join to entries via `sources[].id`. Supersedes `citations` and the body `# Citations` convention.",
                    "items": {
                        "$ref": "#/$defs/Source"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "stale_after": {
                    "description": "Absolute date (YYYY-MM-DD) on or after which the concept is stale (stale when today >= stale_after). schema:expires is the exact concept \u2014 a planned future expiry; prov:invalidatedAtTime would falsely assert the entity WAS invalidated, and dcterms:valid names a validity period only ambiguously.",
                    "format": "date",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "status": {
                    "$ref": "#/$defs/ConceptStatus",
                    "description": "Lifecycle status of the concept: draft | stable | deprecated. Absent means stable (consumer-side default; absence is deliberately distinguishable from an explicit `status: stable`, so no ifabsent). Advisory, never access control."
                },
                "tags": {
                    "description": "Short cross-cutting keywords for categorization.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "timestamp": {
                    "description": "ISO 8601 datetime of the last meaningful change.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "title": {
                    "description": "Human-readable display name.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "type": {
                    "description": "The concept's type. OKF's single required field. In LOKF the value SHOULD name a LOKF class (e.g. Metric, Dataset, Table, Playbook, GlossaryTerm); it designates the JSON-LD @type / rdf:type. Consumers MUST tolerate unknown values by treating the concept as a generic lokf:Concept.",
                    "enum": [
                        "Metric"
                    ],
                    "type": "string"
                },
                "unit": {
                    "description": "The unit of measurement (e.g. USD, seconds, count).",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "usage_window": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/UsageWindow"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "The `{ from, to }` date range framing every `usage_count`. Attached both to Concept (the shared window, sibling of `sources`) and to Source (a per-entry override)."
                },
                "verified": {
                    "description": "Verification events confirming the content against its sources or resource. Independent of `generated`: who wrote a concept need not be who confirmed it. Trust tiers derive from the actors: absent = unverified; only non-human actors = machine-confirmed; any `human:<id>` actor = human-reviewed. A bare `{ by, at }` mapping MUST be read as a one-element list; the LOKF loader normalizes the bare form before LinkML validation. Minted in lokf: no established vocabulary has an entity-to-verification-event property.",
                    "items": {
                        "$ref": "#/$defs/Verification"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "version": {
                    "description": "A version string for the concept's content.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "required": [
                "id",
                "type"
            ],
            "title": "Metric",
            "type": "object"
        },
        "Organization": {
            "additionalProperties": false,
            "description": "An organization, team, or group. Usable both as an inline Agent value (publisher) and as a standalone Concept document with a body and typed relations.",
            "properties": {
                "about": {
                    "description": "The subject matter this concept is about.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "author": {
                    "description": "The agent(s) responsible for authoring the concept.",
                    "items": {
                        "anyOf": [
                            {
                                "$ref": "#/$defs/Person"
                            },
                            {
                                "$ref": "#/$defs/Organization"
                            }
                        ]
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "body": {
                    "description": "The markdown body of the concept document (everything after the frontmatter). Mapped to schema:text; carried as a field only in the JSON-LD / JSON serialization, not duplicated in the frontmatter.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "citations": {
                    "description": "External sources supporting claims in the body.",
                    "items": {
                        "$ref": "#/$defs/Citation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "created": {
                    "description": "ISO 8601 datetime the concept was created.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "definedBy": {
                    "description": "A resource that formally defines this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "dependsOn": {
                    "description": "A concept this concept depends on.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "derivedFrom": {
                    "description": "An entity this concept was derived from (provenance).",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "description": {
                    "description": "A single-sentence summary of the concept.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "email": {
                    "description": "Contact email address.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "generated": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/Generation"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "How and when the current content was produced: `by` (REQUIRED actor) and `at` (ISO 8601 datetime). Supersedes the v0.1 `timestamp` field; consumers MAY fall back to `timestamp` when `generated` is absent."
                },
                "genre": {
                    "$ref": "#/$defs/DiataxisMode",
                    "description": "The Di\u00e1taxis documentation mode of the concept's markdown body (tutorial, how-to, reference, or explanation). An optional facet that is orthogonal to `type`: `type` says what the concept is *about*, `genre` says how its prose *serves the reader*. Consumers MUST tolerate absent or unknown values."
                },
                "hasPart": {
                    "description": "A concept that is a part of this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "id": {
                    "description": "The concept's stable IRI. By convention it is the bundle base IRI joined with the Concept ID (the file path within the bundle, minus the `.md` suffix). Becomes the JSON-LD @id / RDF subject.",
                    "type": "string"
                },
                "isPartOf": {
                    "description": "The target concept that this concept is a part of.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "license": {
                    "description": "The license under which the concept or bundle is offered.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "name": {
                    "description": "A name.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "references": {
                    "description": "A concept or resource this concept refers to.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relatedTo": {
                    "description": "A generic association to another concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relations": {
                    "description": "Typed relationships whose predicate is not covered by a named relation slot. Each entry is a reified Relation (predicate + target).",
                    "items": {
                        "$ref": "#/$defs/Relation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "resource": {
                    "description": "A URI that uniquely identifies the underlying real-world asset the concept describes (a table console URL, an API base URL, etc.). Absent for purely abstract concepts.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "sameAs": {
                    "description": "An IRI asserting this concept is the same entity as another.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "source": {
                    "description": "A resource from which this concept is derived or sourced.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "sources": {
                    "description": "The materials this concept derives from, each carrying objective credibility signals. Markdown footnotes join to entries via `sources[].id`. Supersedes `citations` and the body `# Citations` convention.",
                    "items": {
                        "$ref": "#/$defs/Source"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "stale_after": {
                    "description": "Absolute date (YYYY-MM-DD) on or after which the concept is stale (stale when today >= stale_after). schema:expires is the exact concept \u2014 a planned future expiry; prov:invalidatedAtTime would falsely assert the entity WAS invalidated, and dcterms:valid names a validity period only ambiguously.",
                    "format": "date",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "status": {
                    "$ref": "#/$defs/ConceptStatus",
                    "description": "Lifecycle status of the concept: draft | stable | deprecated. Absent means stable (consumer-side default; absence is deliberately distinguishable from an explicit `status: stable`, so no ifabsent). Advisory, never access control."
                },
                "tags": {
                    "description": "Short cross-cutting keywords for categorization.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "timestamp": {
                    "description": "ISO 8601 datetime of the last meaningful change.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "title": {
                    "description": "Human-readable display name.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "type": {
                    "description": "The concept's type. OKF's single required field. In LOKF the value SHOULD name a LOKF class (e.g. Metric, Dataset, Table, Playbook, GlossaryTerm); it designates the JSON-LD @type / rdf:type. Consumers MUST tolerate unknown values by treating the concept as a generic lokf:Concept.",
                    "enum": [
                        "Organization"
                    ],
                    "type": "string"
                },
                "usage_window": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/UsageWindow"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "The `{ from, to }` date range framing every `usage_count`. Attached both to Concept (the shared window, sibling of `sources`) and to Source (a per-entry override)."
                },
                "verified": {
                    "description": "Verification events confirming the content against its sources or resource. Independent of `generated`: who wrote a concept need not be who confirmed it. Trust tiers derive from the actors: absent = unverified; only non-human actors = machine-confirmed; any `human:<id>` actor = human-reviewed. A bare `{ by, at }` mapping MUST be read as a one-element list; the LOKF loader normalizes the bare form before LinkML validation. Minted in lokf: no established vocabulary has an entity-to-verification-event property.",
                    "items": {
                        "$ref": "#/$defs/Verification"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "version": {
                    "description": "A version string for the concept's content.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "required": [
                "id",
                "type"
            ],
            "title": "Organization",
            "type": "object"
        },
        "Parameter": {
            "additionalProperties": false,
            "description": "A typed, named hole in an Attested Computation that the agent may fill (OKF v0.2 \u00a710.2). Binding semantics (SQL bind variable, dbt var, Python argument) follow the concept's `runtime`. The `type` key deliberately shares the global JSON-LD @type alias \u2014 see the attribute comment for the exact triple that results.",
            "properties": {
                "name": {
                    "description": "The parameter's name, as the runtime addresses it.",
                    "type": "string"
                },
                "required": {
                    "description": "Whether a value for the parameter must be supplied.",
                    "type": [
                        "boolean",
                        "null"
                    ]
                },
                "type": {
                    "$ref": "#/$defs/ParameterType",
                    "description": "The parameter's datatype, written with the authoring key `type` verbatim and drawn from ParameterType. Declared as a class-local attribute rather than slot_usage over the global type slot, because LinkML's falsy-merge silently drops a `designates_type: false` override and gen-python then hard-fails. Because the flat context aliases `type` to @type, the value expands as the node's rdf:type; every ParameterType value therefore means a designed lokf Parameter-kind class, so `type: integer` yields the true, OWL-DL-safe triple `_:p rdf:type lokf:IntegerParameter`. slot_uri rdf:type makes the LinkML RDF-dump path emit the same triple (enum meanings expand to their IRIs)."
                }
            },
            "required": [
                "name"
            ],
            "title": "Parameter",
            "type": "object"
        },
        "ParameterType": {
            "description": "Datatypes for Attested Computation parameters. The permissible values mirror FieldType's authoring surface exactly (one datatype vocabulary for authors), but the meanings deliberately diverge: ParameterType values are written under the `type` key, whose flat-context alias is @type, so each value means a designed lokf Parameter-kind CLASS rather than an XSD datatype \u2014 `_:p rdf:type xsd:integer` would falsely type a non-literal node with a datatype (a class of literals), while `_:p rdf:type lokf:IntegerParameter` is true and OWL-DL-safe. Each value's XSD value space is carried as an annotation and asserted in the generated ontology, where every Parameter-kind class is declared a subclass of lokf:Parameter.",
            "enum": [
                "string",
                "integer",
                "number",
                "boolean",
                "date",
                "datetime",
                "time",
                "uri",
                "json"
            ],
            "title": "ParameterType",
            "type": "string"
        },
        "Person": {
            "additionalProperties": false,
            "description": "An individual person. Usable both as an inline Agent value (author, publisher) and as a standalone Concept document with a body and typed relations.",
            "properties": {
                "about": {
                    "description": "The subject matter this concept is about.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "author": {
                    "description": "The agent(s) responsible for authoring the concept.",
                    "items": {
                        "anyOf": [
                            {
                                "$ref": "#/$defs/Person"
                            },
                            {
                                "$ref": "#/$defs/Organization"
                            }
                        ]
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "body": {
                    "description": "The markdown body of the concept document (everything after the frontmatter). Mapped to schema:text; carried as a field only in the JSON-LD / JSON serialization, not duplicated in the frontmatter.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "citations": {
                    "description": "External sources supporting claims in the body.",
                    "items": {
                        "$ref": "#/$defs/Citation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "created": {
                    "description": "ISO 8601 datetime the concept was created.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "definedBy": {
                    "description": "A resource that formally defines this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "dependsOn": {
                    "description": "A concept this concept depends on.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "derivedFrom": {
                    "description": "An entity this concept was derived from (provenance).",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "description": {
                    "description": "A single-sentence summary of the concept.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "email": {
                    "description": "Contact email address.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "generated": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/Generation"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "How and when the current content was produced: `by` (REQUIRED actor) and `at` (ISO 8601 datetime). Supersedes the v0.1 `timestamp` field; consumers MAY fall back to `timestamp` when `generated` is absent."
                },
                "genre": {
                    "$ref": "#/$defs/DiataxisMode",
                    "description": "The Di\u00e1taxis documentation mode of the concept's markdown body (tutorial, how-to, reference, or explanation). An optional facet that is orthogonal to `type`: `type` says what the concept is *about*, `genre` says how its prose *serves the reader*. Consumers MUST tolerate absent or unknown values."
                },
                "hasPart": {
                    "description": "A concept that is a part of this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "id": {
                    "description": "The concept's stable IRI. By convention it is the bundle base IRI joined with the Concept ID (the file path within the bundle, minus the `.md` suffix). Becomes the JSON-LD @id / RDF subject.",
                    "type": "string"
                },
                "isPartOf": {
                    "description": "The target concept that this concept is a part of.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "license": {
                    "description": "The license under which the concept or bundle is offered.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "name": {
                    "description": "A name.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "references": {
                    "description": "A concept or resource this concept refers to.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relatedTo": {
                    "description": "A generic association to another concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relations": {
                    "description": "Typed relationships whose predicate is not covered by a named relation slot. Each entry is a reified Relation (predicate + target).",
                    "items": {
                        "$ref": "#/$defs/Relation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "resource": {
                    "description": "A URI that uniquely identifies the underlying real-world asset the concept describes (a table console URL, an API base URL, etc.). Absent for purely abstract concepts.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "sameAs": {
                    "description": "An IRI asserting this concept is the same entity as another.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "source": {
                    "description": "A resource from which this concept is derived or sourced.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "sources": {
                    "description": "The materials this concept derives from, each carrying objective credibility signals. Markdown footnotes join to entries via `sources[].id`. Supersedes `citations` and the body `# Citations` convention.",
                    "items": {
                        "$ref": "#/$defs/Source"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "stale_after": {
                    "description": "Absolute date (YYYY-MM-DD) on or after which the concept is stale (stale when today >= stale_after). schema:expires is the exact concept \u2014 a planned future expiry; prov:invalidatedAtTime would falsely assert the entity WAS invalidated, and dcterms:valid names a validity period only ambiguously.",
                    "format": "date",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "status": {
                    "$ref": "#/$defs/ConceptStatus",
                    "description": "Lifecycle status of the concept: draft | stable | deprecated. Absent means stable (consumer-side default; absence is deliberately distinguishable from an explicit `status: stable`, so no ifabsent). Advisory, never access control."
                },
                "tags": {
                    "description": "Short cross-cutting keywords for categorization.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "timestamp": {
                    "description": "ISO 8601 datetime of the last meaningful change.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "title": {
                    "description": "Human-readable display name.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "type": {
                    "description": "The concept's type. OKF's single required field. In LOKF the value SHOULD name a LOKF class (e.g. Metric, Dataset, Table, Playbook, GlossaryTerm); it designates the JSON-LD @type / rdf:type. Consumers MUST tolerate unknown values by treating the concept as a generic lokf:Concept.",
                    "enum": [
                        "Person"
                    ],
                    "type": "string"
                },
                "usage_window": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/UsageWindow"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "The `{ from, to }` date range framing every `usage_count`. Attached both to Concept (the shared window, sibling of `sources`) and to Source (a per-entry override)."
                },
                "verified": {
                    "description": "Verification events confirming the content against its sources or resource. Independent of `generated`: who wrote a concept need not be who confirmed it. Trust tiers derive from the actors: absent = unverified; only non-human actors = machine-confirmed; any `human:<id>` actor = human-reviewed. A bare `{ by, at }` mapping MUST be read as a one-element list; the LOKF loader normalizes the bare form before LinkML validation. Minted in lokf: no established vocabulary has an entity-to-verification-event property.",
                    "items": {
                        "$ref": "#/$defs/Verification"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "version": {
                    "description": "A version string for the concept's content.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "required": [
                "id",
                "type"
            ],
            "title": "Person",
            "type": "object"
        },
        "Playbook": {
            "additionalProperties": false,
            "description": "A procedure or runbook \u2014 an ordered set of steps to accomplish a task or respond to an event. Serves the Di\u00e1taxis how-to (goal-oriented) mode.",
            "properties": {
                "about": {
                    "description": "The subject matter this concept is about.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "author": {
                    "description": "The agent(s) responsible for authoring the concept.",
                    "items": {
                        "anyOf": [
                            {
                                "$ref": "#/$defs/Person"
                            },
                            {
                                "$ref": "#/$defs/Organization"
                            }
                        ]
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "body": {
                    "description": "The markdown body of the concept document (everything after the frontmatter). Mapped to schema:text; carried as a field only in the JSON-LD / JSON serialization, not duplicated in the frontmatter.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "citations": {
                    "description": "External sources supporting claims in the body.",
                    "items": {
                        "$ref": "#/$defs/Citation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "created": {
                    "description": "ISO 8601 datetime the concept was created.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "definedBy": {
                    "description": "A resource that formally defines this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "dependsOn": {
                    "description": "A concept this concept depends on.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "derivedFrom": {
                    "description": "An entity this concept was derived from (provenance).",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "description": {
                    "description": "A single-sentence summary of the concept.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "generated": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/Generation"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "How and when the current content was produced: `by` (REQUIRED actor) and `at` (ISO 8601 datetime). Supersedes the v0.1 `timestamp` field; consumers MAY fall back to `timestamp` when `generated` is absent."
                },
                "genre": {
                    "$ref": "#/$defs/DiataxisMode",
                    "description": "The Di\u00e1taxis documentation mode of the concept's markdown body (tutorial, how-to, reference, or explanation). An optional facet that is orthogonal to `type`: `type` says what the concept is *about*, `genre` says how its prose *serves the reader*. Consumers MUST tolerate absent or unknown values."
                },
                "hasPart": {
                    "description": "A concept that is a part of this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "id": {
                    "description": "The concept's stable IRI. By convention it is the bundle base IRI joined with the Concept ID (the file path within the bundle, minus the `.md` suffix). Becomes the JSON-LD @id / RDF subject.",
                    "type": "string"
                },
                "isPartOf": {
                    "description": "The target concept that this concept is a part of.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "license": {
                    "description": "The license under which the concept or bundle is offered.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "references": {
                    "description": "A concept or resource this concept refers to.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relatedTo": {
                    "description": "A generic association to another concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relations": {
                    "description": "Typed relationships whose predicate is not covered by a named relation slot. Each entry is a reified Relation (predicate + target).",
                    "items": {
                        "$ref": "#/$defs/Relation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "resource": {
                    "description": "A URI that uniquely identifies the underlying real-world asset the concept describes (a table console URL, an API base URL, etc.). Absent for purely abstract concepts.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "sameAs": {
                    "description": "An IRI asserting this concept is the same entity as another.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "source": {
                    "description": "A resource from which this concept is derived or sourced.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "sources": {
                    "description": "The materials this concept derives from, each carrying objective credibility signals. Markdown footnotes join to entries via `sources[].id`. Supersedes `citations` and the body `# Citations` convention.",
                    "items": {
                        "$ref": "#/$defs/Source"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "stale_after": {
                    "description": "Absolute date (YYYY-MM-DD) on or after which the concept is stale (stale when today >= stale_after). schema:expires is the exact concept \u2014 a planned future expiry; prov:invalidatedAtTime would falsely assert the entity WAS invalidated, and dcterms:valid names a validity period only ambiguously.",
                    "format": "date",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "status": {
                    "$ref": "#/$defs/ConceptStatus",
                    "description": "Lifecycle status of the concept: draft | stable | deprecated. Absent means stable (consumer-side default; absence is deliberately distinguishable from an explicit `status: stable`, so no ifabsent). Advisory, never access control."
                },
                "tags": {
                    "description": "Short cross-cutting keywords for categorization.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "timestamp": {
                    "description": "ISO 8601 datetime of the last meaningful change.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "title": {
                    "description": "Human-readable display name.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "type": {
                    "description": "The concept's type. OKF's single required field. In LOKF the value SHOULD name a LOKF class (e.g. Metric, Dataset, Table, Playbook, GlossaryTerm); it designates the JSON-LD @type / rdf:type. Consumers MUST tolerate unknown values by treating the concept as a generic lokf:Concept.",
                    "enum": [
                        "Playbook"
                    ],
                    "type": "string"
                },
                "usage_window": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/UsageWindow"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "The `{ from, to }` date range framing every `usage_count`. Attached both to Concept (the shared window, sibling of `sources`) and to Source (a per-entry override)."
                },
                "verified": {
                    "description": "Verification events confirming the content against its sources or resource. Independent of `generated`: who wrote a concept need not be who confirmed it. Trust tiers derive from the actors: absent = unverified; only non-human actors = machine-confirmed; any `human:<id>` actor = human-reviewed. A bare `{ by, at }` mapping MUST be read as a one-element list; the LOKF loader normalizes the bare form before LinkML validation. Minted in lokf: no established vocabulary has an entity-to-verification-event property.",
                    "items": {
                        "$ref": "#/$defs/Verification"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "version": {
                    "description": "A version string for the concept's content.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "required": [
                "id",
                "type"
            ],
            "title": "Playbook",
            "type": "object"
        },
        "Policy": {
            "additionalProperties": false,
            "description": "A governance, compliance, or operational policy document.",
            "properties": {
                "about": {
                    "description": "The subject matter this concept is about.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "author": {
                    "description": "The agent(s) responsible for authoring the concept.",
                    "items": {
                        "anyOf": [
                            {
                                "$ref": "#/$defs/Person"
                            },
                            {
                                "$ref": "#/$defs/Organization"
                            }
                        ]
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "body": {
                    "description": "The markdown body of the concept document (everything after the frontmatter). Mapped to schema:text; carried as a field only in the JSON-LD / JSON serialization, not duplicated in the frontmatter.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "citations": {
                    "description": "External sources supporting claims in the body.",
                    "items": {
                        "$ref": "#/$defs/Citation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "created": {
                    "description": "ISO 8601 datetime the concept was created.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "definedBy": {
                    "description": "A resource that formally defines this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "dependsOn": {
                    "description": "A concept this concept depends on.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "derivedFrom": {
                    "description": "An entity this concept was derived from (provenance).",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "description": {
                    "description": "A single-sentence summary of the concept.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "generated": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/Generation"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "How and when the current content was produced: `by` (REQUIRED actor) and `at` (ISO 8601 datetime). Supersedes the v0.1 `timestamp` field; consumers MAY fall back to `timestamp` when `generated` is absent."
                },
                "genre": {
                    "$ref": "#/$defs/DiataxisMode",
                    "description": "The Di\u00e1taxis documentation mode of the concept's markdown body (tutorial, how-to, reference, or explanation). An optional facet that is orthogonal to `type`: `type` says what the concept is *about*, `genre` says how its prose *serves the reader*. Consumers MUST tolerate absent or unknown values."
                },
                "hasPart": {
                    "description": "A concept that is a part of this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "id": {
                    "description": "The concept's stable IRI. By convention it is the bundle base IRI joined with the Concept ID (the file path within the bundle, minus the `.md` suffix). Becomes the JSON-LD @id / RDF subject.",
                    "type": "string"
                },
                "isPartOf": {
                    "description": "The target concept that this concept is a part of.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "license": {
                    "description": "The license under which the concept or bundle is offered.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "references": {
                    "description": "A concept or resource this concept refers to.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relatedTo": {
                    "description": "A generic association to another concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relations": {
                    "description": "Typed relationships whose predicate is not covered by a named relation slot. Each entry is a reified Relation (predicate + target).",
                    "items": {
                        "$ref": "#/$defs/Relation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "resource": {
                    "description": "A URI that uniquely identifies the underlying real-world asset the concept describes (a table console URL, an API base URL, etc.). Absent for purely abstract concepts.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "sameAs": {
                    "description": "An IRI asserting this concept is the same entity as another.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "source": {
                    "description": "A resource from which this concept is derived or sourced.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "sources": {
                    "description": "The materials this concept derives from, each carrying objective credibility signals. Markdown footnotes join to entries via `sources[].id`. Supersedes `citations` and the body `# Citations` convention.",
                    "items": {
                        "$ref": "#/$defs/Source"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "stale_after": {
                    "description": "Absolute date (YYYY-MM-DD) on or after which the concept is stale (stale when today >= stale_after). schema:expires is the exact concept \u2014 a planned future expiry; prov:invalidatedAtTime would falsely assert the entity WAS invalidated, and dcterms:valid names a validity period only ambiguously.",
                    "format": "date",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "status": {
                    "$ref": "#/$defs/ConceptStatus",
                    "description": "Lifecycle status of the concept: draft | stable | deprecated. Absent means stable (consumer-side default; absence is deliberately distinguishable from an explicit `status: stable`, so no ifabsent). Advisory, never access control."
                },
                "tags": {
                    "description": "Short cross-cutting keywords for categorization.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "timestamp": {
                    "description": "ISO 8601 datetime of the last meaningful change.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "title": {
                    "description": "Human-readable display name.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "type": {
                    "description": "The concept's type. OKF's single required field. In LOKF the value SHOULD name a LOKF class (e.g. Metric, Dataset, Table, Playbook, GlossaryTerm); it designates the JSON-LD @type / rdf:type. Consumers MUST tolerate unknown values by treating the concept as a generic lokf:Concept.",
                    "enum": [
                        "Policy"
                    ],
                    "type": "string"
                },
                "usage_window": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/UsageWindow"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "The `{ from, to }` date range framing every `usage_count`. Attached both to Concept (the shared window, sibling of `sources`) and to Source (a per-entry override)."
                },
                "verified": {
                    "description": "Verification events confirming the content against its sources or resource. Independent of `generated`: who wrote a concept need not be who confirmed it. Trust tiers derive from the actors: absent = unverified; only non-human actors = machine-confirmed; any `human:<id>` actor = human-reviewed. A bare `{ by, at }` mapping MUST be read as a one-element list; the LOKF loader normalizes the bare form before LinkML validation. Minted in lokf: no established vocabulary has an entity-to-verification-event property.",
                    "items": {
                        "$ref": "#/$defs/Verification"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "version": {
                    "description": "A version string for the concept's content.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "required": [
                "id",
                "type"
            ],
            "title": "Policy",
            "type": "object"
        },
        "Reference": {
            "additionalProperties": false,
            "description": "A concept that mirrors an external source (a page, paper, or document) as a first-class citizen of the bundle so it can be cited and linked.",
            "properties": {
                "about": {
                    "description": "The subject matter this concept is about.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "author": {
                    "description": "The agent(s) responsible for authoring the concept.",
                    "items": {
                        "anyOf": [
                            {
                                "$ref": "#/$defs/Person"
                            },
                            {
                                "$ref": "#/$defs/Organization"
                            }
                        ]
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "body": {
                    "description": "The markdown body of the concept document (everything after the frontmatter). Mapped to schema:text; carried as a field only in the JSON-LD / JSON serialization, not duplicated in the frontmatter.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "citations": {
                    "description": "External sources supporting claims in the body.",
                    "items": {
                        "$ref": "#/$defs/Citation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "created": {
                    "description": "ISO 8601 datetime the concept was created.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "definedBy": {
                    "description": "A resource that formally defines this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "dependsOn": {
                    "description": "A concept this concept depends on.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "derivedFrom": {
                    "description": "An entity this concept was derived from (provenance).",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "description": {
                    "description": "A single-sentence summary of the concept.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "generated": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/Generation"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "How and when the current content was produced: `by` (REQUIRED actor) and `at` (ISO 8601 datetime). Supersedes the v0.1 `timestamp` field; consumers MAY fall back to `timestamp` when `generated` is absent."
                },
                "genre": {
                    "$ref": "#/$defs/DiataxisMode",
                    "description": "The Di\u00e1taxis documentation mode of the concept's markdown body (tutorial, how-to, reference, or explanation). An optional facet that is orthogonal to `type`: `type` says what the concept is *about*, `genre` says how its prose *serves the reader*. Consumers MUST tolerate absent or unknown values."
                },
                "hasPart": {
                    "description": "A concept that is a part of this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "id": {
                    "description": "The concept's stable IRI. By convention it is the bundle base IRI joined with the Concept ID (the file path within the bundle, minus the `.md` suffix). Becomes the JSON-LD @id / RDF subject.",
                    "type": "string"
                },
                "isPartOf": {
                    "description": "The target concept that this concept is a part of.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "license": {
                    "description": "The license under which the concept or bundle is offered.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "references": {
                    "description": "A concept or resource this concept refers to.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relatedTo": {
                    "description": "A generic association to another concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relations": {
                    "description": "Typed relationships whose predicate is not covered by a named relation slot. Each entry is a reified Relation (predicate + target).",
                    "items": {
                        "$ref": "#/$defs/Relation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "resource": {
                    "description": "A URI that uniquely identifies the underlying real-world asset the concept describes (a table console URL, an API base URL, etc.). Absent for purely abstract concepts.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "sameAs": {
                    "description": "An IRI asserting this concept is the same entity as another.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "source": {
                    "description": "A resource from which this concept is derived or sourced.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "sources": {
                    "description": "The materials this concept derives from, each carrying objective credibility signals. Markdown footnotes join to entries via `sources[].id`. Supersedes `citations` and the body `# Citations` convention.",
                    "items": {
                        "$ref": "#/$defs/Source"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "stale_after": {
                    "description": "Absolute date (YYYY-MM-DD) on or after which the concept is stale (stale when today >= stale_after). schema:expires is the exact concept \u2014 a planned future expiry; prov:invalidatedAtTime would falsely assert the entity WAS invalidated, and dcterms:valid names a validity period only ambiguously.",
                    "format": "date",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "status": {
                    "$ref": "#/$defs/ConceptStatus",
                    "description": "Lifecycle status of the concept: draft | stable | deprecated. Absent means stable (consumer-side default; absence is deliberately distinguishable from an explicit `status: stable`, so no ifabsent). Advisory, never access control."
                },
                "tags": {
                    "description": "Short cross-cutting keywords for categorization.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "timestamp": {
                    "description": "ISO 8601 datetime of the last meaningful change.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "title": {
                    "description": "Human-readable display name.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "type": {
                    "description": "The concept's type. OKF's single required field. In LOKF the value SHOULD name a LOKF class (e.g. Metric, Dataset, Table, Playbook, GlossaryTerm); it designates the JSON-LD @type / rdf:type. Consumers MUST tolerate unknown values by treating the concept as a generic lokf:Concept.",
                    "enum": [
                        "Reference"
                    ],
                    "type": "string"
                },
                "usage_window": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/UsageWindow"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "The `{ from, to }` date range framing every `usage_count`. Attached both to Concept (the shared window, sibling of `sources`) and to Source (a per-entry override)."
                },
                "verified": {
                    "description": "Verification events confirming the content against its sources or resource. Independent of `generated`: who wrote a concept need not be who confirmed it. Trust tiers derive from the actors: absent = unverified; only non-human actors = machine-confirmed; any `human:<id>` actor = human-reviewed. A bare `{ by, at }` mapping MUST be read as a one-element list; the LOKF loader normalizes the bare form before LinkML validation. Minted in lokf: no established vocabulary has an entity-to-verification-event property.",
                    "items": {
                        "$ref": "#/$defs/Verification"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "version": {
                    "description": "A version string for the concept's content.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "required": [
                "id",
                "type"
            ],
            "title": "Reference",
            "type": "object"
        },
        "Relation": {
            "additionalProperties": false,
            "description": "A typed, reified relationship from the containing concept to a target, used when a predicate is not covered by one of the named relation slots. Modeled as an rdf:Statement (subject = the containing concept).",
            "properties": {
                "predicate": {
                    "$ref": "#/$defs/RelationType",
                    "description": "The relationship type. SHOULD be drawn from the RelationType vocabulary."
                },
                "relation_label": {
                    "description": "An optional human-readable label for the relationship.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "target": {
                    "description": "The IRI (or Concept ID) of the relationship's object.",
                    "type": "string"
                }
            },
            "required": [
                "predicate",
                "target"
            ],
            "title": "Relation",
            "type": "object"
        },
        "RelationType": {
            "description": "The recommended controlled vocabulary of relationship predicates for reified Relations. Each value carries a `meaning` mapping to its RDF predicate in schema.org, Dublin Core Terms, PROV-O, RDFS, or OWL.",
            "enum": [
                "isPartOf",
                "hasPart",
                "references",
                "dependsOn",
                "derivedFrom",
                "about",
                "sameAs",
                "relatedTo",
                "wasAttributedTo",
                "definedBy",
                "measures",
                "joinsWith",
                "source",
                "memberOf",
                "holder"
            ],
            "title": "RelationType",
            "type": "string"
        },
        "Role": {
            "additionalProperties": false,
            "description": "A role an agent holds within an organization over a period of time \u2014 a job, appointment, or position. Reifies the agent\u2013organization link so it can carry a title (roleName) and a start/end interval, following the schema.org Role and W3C ORG Membership patterns.",
            "properties": {
                "about": {
                    "description": "The subject matter this concept is about.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "author": {
                    "description": "The agent(s) responsible for authoring the concept.",
                    "items": {
                        "anyOf": [
                            {
                                "$ref": "#/$defs/Person"
                            },
                            {
                                "$ref": "#/$defs/Organization"
                            }
                        ]
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "body": {
                    "description": "The markdown body of the concept document (everything after the frontmatter). Mapped to schema:text; carried as a field only in the JSON-LD / JSON serialization, not duplicated in the frontmatter.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "citations": {
                    "description": "External sources supporting claims in the body.",
                    "items": {
                        "$ref": "#/$defs/Citation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "created": {
                    "description": "ISO 8601 datetime the concept was created.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "definedBy": {
                    "description": "A resource that formally defines this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "dependsOn": {
                    "description": "A concept this concept depends on.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "derivedFrom": {
                    "description": "An entity this concept was derived from (provenance).",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "description": {
                    "description": "A single-sentence summary of the concept.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "endDate": {
                    "description": "The date the role ended; omit for a role still held.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "generated": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/Generation"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "How and when the current content was produced: `by` (REQUIRED actor) and `at` (ISO 8601 datetime). Supersedes the v0.1 `timestamp` field; consumers MAY fall back to `timestamp` when `generated` is absent."
                },
                "genre": {
                    "$ref": "#/$defs/DiataxisMode",
                    "description": "The Di\u00e1taxis documentation mode of the concept's markdown body (tutorial, how-to, reference, or explanation). An optional facet that is orthogonal to `type`: `type` says what the concept is *about*, `genre` says how its prose *serves the reader*. Consumers MUST tolerate absent or unknown values."
                },
                "hasPart": {
                    "description": "A concept that is a part of this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "holder": {
                    "description": "The agent (typically a Person) who holds the role.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "id": {
                    "description": "The concept's stable IRI. By convention it is the bundle base IRI joined with the Concept ID (the file path within the bundle, minus the `.md` suffix). Becomes the JSON-LD @id / RDF subject.",
                    "type": "string"
                },
                "isPartOf": {
                    "description": "The target concept that this concept is a part of.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "license": {
                    "description": "The license under which the concept or bundle is offered.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "memberOf": {
                    "description": "The organization within which the role is held.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "references": {
                    "description": "A concept or resource this concept refers to.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relatedTo": {
                    "description": "A generic association to another concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relations": {
                    "description": "Typed relationships whose predicate is not covered by a named relation slot. Each entry is a reified Relation (predicate + target).",
                    "items": {
                        "$ref": "#/$defs/Relation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "resource": {
                    "description": "A URI that uniquely identifies the underlying real-world asset the concept describes (a table console URL, an API base URL, etc.). Absent for purely abstract concepts.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "roleName": {
                    "description": "The name/title of the role held (e.g. a job title).",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "sameAs": {
                    "description": "An IRI asserting this concept is the same entity as another.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "source": {
                    "description": "A resource from which this concept is derived or sourced.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "sources": {
                    "description": "The materials this concept derives from, each carrying objective credibility signals. Markdown footnotes join to entries via `sources[].id`. Supersedes `citations` and the body `# Citations` convention.",
                    "items": {
                        "$ref": "#/$defs/Source"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "stale_after": {
                    "description": "Absolute date (YYYY-MM-DD) on or after which the concept is stale (stale when today >= stale_after). schema:expires is the exact concept \u2014 a planned future expiry; prov:invalidatedAtTime would falsely assert the entity WAS invalidated, and dcterms:valid names a validity period only ambiguously.",
                    "format": "date",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "startDate": {
                    "description": "The date the role began (ISO 8601 date or year).",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "status": {
                    "$ref": "#/$defs/ConceptStatus",
                    "description": "Lifecycle status of the concept: draft | stable | deprecated. Absent means stable (consumer-side default; absence is deliberately distinguishable from an explicit `status: stable`, so no ifabsent). Advisory, never access control."
                },
                "tags": {
                    "description": "Short cross-cutting keywords for categorization.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "timestamp": {
                    "description": "ISO 8601 datetime of the last meaningful change.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "title": {
                    "description": "Human-readable display name.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "type": {
                    "description": "The concept's type. OKF's single required field. In LOKF the value SHOULD name a LOKF class (e.g. Metric, Dataset, Table, Playbook, GlossaryTerm); it designates the JSON-LD @type / rdf:type. Consumers MUST tolerate unknown values by treating the concept as a generic lokf:Concept.",
                    "enum": [
                        "Role"
                    ],
                    "type": "string"
                },
                "usage_window": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/UsageWindow"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "The `{ from, to }` date range framing every `usage_count`. Attached both to Concept (the shared window, sibling of `sources`) and to Source (a per-entry override)."
                },
                "verified": {
                    "description": "Verification events confirming the content against its sources or resource. Independent of `generated`: who wrote a concept need not be who confirmed it. Trust tiers derive from the actors: absent = unverified; only non-human actors = machine-confirmed; any `human:<id>` actor = human-reviewed. A bare `{ by, at }` mapping MUST be read as a one-element list; the LOKF loader normalizes the bare form before LinkML validation. Minted in lokf: no established vocabulary has an entity-to-verification-event property.",
                    "items": {
                        "$ref": "#/$defs/Verification"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "version": {
                    "description": "A version string for the concept's content.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "required": [
                "id",
                "type"
            ],
            "title": "Role",
            "type": "object"
        },
        "Service": {
            "additionalProperties": false,
            "description": "A callable service or API endpoint.",
            "properties": {
                "about": {
                    "description": "The subject matter this concept is about.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "author": {
                    "description": "The agent(s) responsible for authoring the concept.",
                    "items": {
                        "anyOf": [
                            {
                                "$ref": "#/$defs/Person"
                            },
                            {
                                "$ref": "#/$defs/Organization"
                            }
                        ]
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "body": {
                    "description": "The markdown body of the concept document (everything after the frontmatter). Mapped to schema:text; carried as a field only in the JSON-LD / JSON serialization, not duplicated in the frontmatter.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "citations": {
                    "description": "External sources supporting claims in the body.",
                    "items": {
                        "$ref": "#/$defs/Citation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "created": {
                    "description": "ISO 8601 datetime the concept was created.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "definedBy": {
                    "description": "A resource that formally defines this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "dependsOn": {
                    "description": "A concept this concept depends on.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "derivedFrom": {
                    "description": "An entity this concept was derived from (provenance).",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "description": {
                    "description": "A single-sentence summary of the concept.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "documentation": {
                    "description": "A link to the service's documentation.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "endpoint": {
                    "description": "The base URL or invocation endpoint of a service.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "generated": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/Generation"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "How and when the current content was produced: `by` (REQUIRED actor) and `at` (ISO 8601 datetime). Supersedes the v0.1 `timestamp` field; consumers MAY fall back to `timestamp` when `generated` is absent."
                },
                "genre": {
                    "$ref": "#/$defs/DiataxisMode",
                    "description": "The Di\u00e1taxis documentation mode of the concept's markdown body (tutorial, how-to, reference, or explanation). An optional facet that is orthogonal to `type`: `type` says what the concept is *about*, `genre` says how its prose *serves the reader*. Consumers MUST tolerate absent or unknown values."
                },
                "hasPart": {
                    "description": "A concept that is a part of this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "http_method": {
                    "description": "The HTTP method used to invoke the service, if applicable.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "id": {
                    "description": "The concept's stable IRI. By convention it is the bundle base IRI joined with the Concept ID (the file path within the bundle, minus the `.md` suffix). Becomes the JSON-LD @id / RDF subject.",
                    "type": "string"
                },
                "isPartOf": {
                    "description": "The target concept that this concept is a part of.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "license": {
                    "description": "The license under which the concept or bundle is offered.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "references": {
                    "description": "A concept or resource this concept refers to.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relatedTo": {
                    "description": "A generic association to another concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relations": {
                    "description": "Typed relationships whose predicate is not covered by a named relation slot. Each entry is a reified Relation (predicate + target).",
                    "items": {
                        "$ref": "#/$defs/Relation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "resource": {
                    "description": "A URI that uniquely identifies the underlying real-world asset the concept describes (a table console URL, an API base URL, etc.). Absent for purely abstract concepts.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "sameAs": {
                    "description": "An IRI asserting this concept is the same entity as another.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "source": {
                    "description": "A resource from which this concept is derived or sourced.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "sources": {
                    "description": "The materials this concept derives from, each carrying objective credibility signals. Markdown footnotes join to entries via `sources[].id`. Supersedes `citations` and the body `# Citations` convention.",
                    "items": {
                        "$ref": "#/$defs/Source"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "stale_after": {
                    "description": "Absolute date (YYYY-MM-DD) on or after which the concept is stale (stale when today >= stale_after). schema:expires is the exact concept \u2014 a planned future expiry; prov:invalidatedAtTime would falsely assert the entity WAS invalidated, and dcterms:valid names a validity period only ambiguously.",
                    "format": "date",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "status": {
                    "$ref": "#/$defs/ConceptStatus",
                    "description": "Lifecycle status of the concept: draft | stable | deprecated. Absent means stable (consumer-side default; absence is deliberately distinguishable from an explicit `status: stable`, so no ifabsent). Advisory, never access control."
                },
                "tags": {
                    "description": "Short cross-cutting keywords for categorization.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "timestamp": {
                    "description": "ISO 8601 datetime of the last meaningful change.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "title": {
                    "description": "Human-readable display name.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "type": {
                    "description": "The concept's type. OKF's single required field. In LOKF the value SHOULD name a LOKF class (e.g. Metric, Dataset, Table, Playbook, GlossaryTerm); it designates the JSON-LD @type / rdf:type. Consumers MUST tolerate unknown values by treating the concept as a generic lokf:Concept.",
                    "enum": [
                        "Service"
                    ],
                    "type": "string"
                },
                "usage_window": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/UsageWindow"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "The `{ from, to }` date range framing every `usage_count`. Attached both to Concept (the shared window, sibling of `sources`) and to Source (a per-entry override)."
                },
                "verified": {
                    "description": "Verification events confirming the content against its sources or resource. Independent of `generated`: who wrote a concept need not be who confirmed it. Trust tiers derive from the actors: absent = unverified; only non-human actors = machine-confirmed; any `human:<id>` actor = human-reviewed. A bare `{ by, at }` mapping MUST be read as a one-element list; the LOKF loader normalizes the bare form before LinkML validation. Minted in lokf: no established vocabulary has an entity-to-verification-event property.",
                    "items": {
                        "$ref": "#/$defs/Verification"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "version": {
                    "description": "A version string for the concept's content.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "required": [
                "id",
                "type"
            ],
            "title": "Service",
            "type": "object"
        },
        "Source": {
            "additionalProperties": false,
            "description": "A material a concept derives from (OKF v0.2 \u00a75.1) \u2014 a pointer (`resource`) plus objective per-source credibility signals (`author`, `usage_count`, `last_modified`) from which consumers infer trust; OKF stores signals, never a score. Markdown footnotes whose label equals `id` attribute individual claims to this entry. Supersedes Citation.",
            "properties": {
                "author": {
                    "description": "Who or what produced the source, as an OKF \u00a77 actor string (e.g. `team:ga4-docs`) \u2014 an authority signal carried as a plain literal, not an inlined Agent object.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "id": {
                    "description": "Optional stable key joining markdown footnote labels to this entry (e.g. `ga4-schema`). Declared as a class-local attribute rather than the global identifier slot so it stays optional (LinkML's falsy-merge silently drops an `identifier: false` slot_usage override and generation then fails). Under the published flat context `id` maps to @id, so the value resolves against the bundle base and mints the source node's IRI \u2014 the same id used across concepts merges into one RDF node; schema:identifier applies only on the LinkML RDF-dump path.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "last_modified": {
                    "description": "When the SOURCE itself last changed (YYYY-MM-DD). A recency signal about the source, distinct from `generated.at`, which records when this concept's content was produced. Same predicate as the deprecated `timestamp`, on a different subject node (source vs concept).",
                    "format": "date",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "resource": {
                    "description": "REQUIRED. An absolute URL, a bundle-relative path, or a population/scope descriptor string (e.g. \"all queries in BigQuery project X\"). Range is widened to string to admit scope descriptors; non-URI values are lossy on the RDF projection (they serialize as xsd:anyURI-typed literals).",
                    "type": "string"
                },
                "title": {
                    "description": "Human-readable display name.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "usage_count": {
                    "description": "How often the source's resource was exercised (views, executions, reads) over the governing usage_window. An adoption/liveness signal read as order-of-magnitude and trend, not a score. Minted in lokf: no established vocabulary has a bare-integer usage property (schema:userInteractionCount is domained to the InteractionCounter reification).",
                    "type": [
                        "integer",
                        "null"
                    ]
                },
                "usage_window": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/UsageWindow"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "The `{ from, to }` date range framing every `usage_count`. Attached both to Concept (the shared window, sibling of `sources`) and to Source (a per-entry override)."
                }
            },
            "required": [
                "resource"
            ],
            "title": "Source",
            "type": "object"
        },
        "Table": {
            "additionalProperties": false,
            "description": "A structured, tabular dataset (e.g. a warehouse table or view) whose columns are described by Field objects under `fields`.",
            "properties": {
                "about": {
                    "description": "The subject matter this concept is about.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "author": {
                    "description": "The agent(s) responsible for authoring the concept.",
                    "items": {
                        "anyOf": [
                            {
                                "$ref": "#/$defs/Person"
                            },
                            {
                                "$ref": "#/$defs/Organization"
                            }
                        ]
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "body": {
                    "description": "The markdown body of the concept document (everything after the frontmatter). Mapped to schema:text; carried as a field only in the JSON-LD / JSON serialization, not duplicated in the frontmatter.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "citations": {
                    "description": "External sources supporting claims in the body.",
                    "items": {
                        "$ref": "#/$defs/Citation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "created": {
                    "description": "ISO 8601 datetime the concept was created.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "definedBy": {
                    "description": "A resource that formally defines this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "dependsOn": {
                    "description": "A concept this concept depends on.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "derivedFrom": {
                    "description": "An entity this concept was derived from (provenance).",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "description": {
                    "description": "A single-sentence summary of the concept.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "distribution": {
                    "description": "Accessible forms of the dataset.",
                    "items": {
                        "$ref": "#/$defs/Distribution"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "fields": {
                    "description": "The columns / properties that make up a dataset's schema.",
                    "items": {
                        "$ref": "#/$defs/Field"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "generated": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/Generation"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "How and when the current content was produced: `by` (REQUIRED actor) and `at` (ISO 8601 datetime). Supersedes the v0.1 `timestamp` field; consumers MAY fall back to `timestamp` when `generated` is absent."
                },
                "genre": {
                    "$ref": "#/$defs/DiataxisMode",
                    "description": "The Di\u00e1taxis documentation mode of the concept's markdown body (tutorial, how-to, reference, or explanation). An optional facet that is orthogonal to `type`: `type` says what the concept is *about*, `genre` says how its prose *serves the reader*. Consumers MUST tolerate absent or unknown values."
                },
                "hasPart": {
                    "description": "A concept that is a part of this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "id": {
                    "description": "The concept's stable IRI. By convention it is the bundle base IRI joined with the Concept ID (the file path within the bundle, minus the `.md` suffix). Becomes the JSON-LD @id / RDF subject.",
                    "type": "string"
                },
                "isPartOf": {
                    "description": "The target concept that this concept is a part of.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "license": {
                    "description": "The license under which the concept or bundle is offered.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "references": {
                    "description": "A concept or resource this concept refers to.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relatedTo": {
                    "description": "A generic association to another concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relations": {
                    "description": "Typed relationships whose predicate is not covered by a named relation slot. Each entry is a reified Relation (predicate + target).",
                    "items": {
                        "$ref": "#/$defs/Relation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "resource": {
                    "description": "A URI that uniquely identifies the underlying real-world asset the concept describes (a table console URL, an API base URL, etc.). Absent for purely abstract concepts.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "sameAs": {
                    "description": "An IRI asserting this concept is the same entity as another.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "source": {
                    "description": "A resource from which this concept is derived or sourced.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "sources": {
                    "description": "The materials this concept derives from, each carrying objective credibility signals. Markdown footnotes join to entries via `sources[].id`. Supersedes `citations` and the body `# Citations` convention.",
                    "items": {
                        "$ref": "#/$defs/Source"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "stale_after": {
                    "description": "Absolute date (YYYY-MM-DD) on or after which the concept is stale (stale when today >= stale_after). schema:expires is the exact concept \u2014 a planned future expiry; prov:invalidatedAtTime would falsely assert the entity WAS invalidated, and dcterms:valid names a validity period only ambiguously.",
                    "format": "date",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "status": {
                    "$ref": "#/$defs/ConceptStatus",
                    "description": "Lifecycle status of the concept: draft | stable | deprecated. Absent means stable (consumer-side default; absence is deliberately distinguishable from an explicit `status: stable`, so no ifabsent). Advisory, never access control."
                },
                "tags": {
                    "description": "Short cross-cutting keywords for categorization.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "timestamp": {
                    "description": "ISO 8601 datetime of the last meaningful change.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "title": {
                    "description": "Human-readable display name.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "type": {
                    "description": "The concept's type. OKF's single required field. In LOKF the value SHOULD name a LOKF class (e.g. Metric, Dataset, Table, Playbook, GlossaryTerm); it designates the JSON-LD @type / rdf:type. Consumers MUST tolerate unknown values by treating the concept as a generic lokf:Concept.",
                    "enum": [
                        "Table"
                    ],
                    "type": "string"
                },
                "usage_window": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/UsageWindow"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "The `{ from, to }` date range framing every `usage_count`. Attached both to Concept (the shared window, sibling of `sources`) and to Source (a per-entry override)."
                },
                "verified": {
                    "description": "Verification events confirming the content against its sources or resource. Independent of `generated`: who wrote a concept need not be who confirmed it. Trust tiers derive from the actors: absent = unverified; only non-human actors = machine-confirmed; any `human:<id>` actor = human-reviewed. A bare `{ by, at }` mapping MUST be read as a one-element list; the LOKF loader normalizes the bare form before LinkML validation. Minted in lokf: no established vocabulary has an entity-to-verification-event property.",
                    "items": {
                        "$ref": "#/$defs/Verification"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "version": {
                    "description": "A version string for the concept's content.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "required": [
                "id",
                "type"
            ],
            "title": "Table",
            "type": "object"
        },
        "Tutorial": {
            "additionalProperties": false,
            "description": "A learning-oriented lesson: a guided, end-to-end practical activity through which a reader acquires a skill by doing. The Di\u00e1taxis tutorial mode as a first-class concept type; typically links to the reference and explanation it draws on with `references`.",
            "properties": {
                "about": {
                    "description": "The subject matter this concept is about.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "author": {
                    "description": "The agent(s) responsible for authoring the concept.",
                    "items": {
                        "anyOf": [
                            {
                                "$ref": "#/$defs/Person"
                            },
                            {
                                "$ref": "#/$defs/Organization"
                            }
                        ]
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "body": {
                    "description": "The markdown body of the concept document (everything after the frontmatter). Mapped to schema:text; carried as a field only in the JSON-LD / JSON serialization, not duplicated in the frontmatter.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "citations": {
                    "description": "External sources supporting claims in the body.",
                    "items": {
                        "$ref": "#/$defs/Citation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "created": {
                    "description": "ISO 8601 datetime the concept was created.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "definedBy": {
                    "description": "A resource that formally defines this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "dependsOn": {
                    "description": "A concept this concept depends on.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "derivedFrom": {
                    "description": "An entity this concept was derived from (provenance).",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "description": {
                    "description": "A single-sentence summary of the concept.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "generated": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/Generation"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "How and when the current content was produced: `by` (REQUIRED actor) and `at` (ISO 8601 datetime). Supersedes the v0.1 `timestamp` field; consumers MAY fall back to `timestamp` when `generated` is absent."
                },
                "genre": {
                    "$ref": "#/$defs/DiataxisMode",
                    "description": "The Di\u00e1taxis documentation mode of the concept's markdown body (tutorial, how-to, reference, or explanation). An optional facet that is orthogonal to `type`: `type` says what the concept is *about*, `genre` says how its prose *serves the reader*. Consumers MUST tolerate absent or unknown values."
                },
                "hasPart": {
                    "description": "A concept that is a part of this concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "id": {
                    "description": "The concept's stable IRI. By convention it is the bundle base IRI joined with the Concept ID (the file path within the bundle, minus the `.md` suffix). Becomes the JSON-LD @id / RDF subject.",
                    "type": "string"
                },
                "isPartOf": {
                    "description": "The target concept that this concept is a part of.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "license": {
                    "description": "The license under which the concept or bundle is offered.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "references": {
                    "description": "A concept or resource this concept refers to.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relatedTo": {
                    "description": "A generic association to another concept.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "relations": {
                    "description": "Typed relationships whose predicate is not covered by a named relation slot. Each entry is a reified Relation (predicate + target).",
                    "items": {
                        "$ref": "#/$defs/Relation"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "resource": {
                    "description": "A URI that uniquely identifies the underlying real-world asset the concept describes (a table console URL, an API base URL, etc.). Absent for purely abstract concepts.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "sameAs": {
                    "description": "An IRI asserting this concept is the same entity as another.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "source": {
                    "description": "A resource from which this concept is derived or sourced.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "sources": {
                    "description": "The materials this concept derives from, each carrying objective credibility signals. Markdown footnotes join to entries via `sources[].id`. Supersedes `citations` and the body `# Citations` convention.",
                    "items": {
                        "$ref": "#/$defs/Source"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "stale_after": {
                    "description": "Absolute date (YYYY-MM-DD) on or after which the concept is stale (stale when today >= stale_after). schema:expires is the exact concept \u2014 a planned future expiry; prov:invalidatedAtTime would falsely assert the entity WAS invalidated, and dcterms:valid names a validity period only ambiguously.",
                    "format": "date",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "status": {
                    "$ref": "#/$defs/ConceptStatus",
                    "description": "Lifecycle status of the concept: draft | stable | deprecated. Absent means stable (consumer-side default; absence is deliberately distinguishable from an explicit `status: stable`, so no ifabsent). Advisory, never access control."
                },
                "tags": {
                    "description": "Short cross-cutting keywords for categorization.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "timestamp": {
                    "description": "ISO 8601 datetime of the last meaningful change.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "title": {
                    "description": "Human-readable display name.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "type": {
                    "description": "The concept's type. OKF's single required field. In LOKF the value SHOULD name a LOKF class (e.g. Metric, Dataset, Table, Playbook, GlossaryTerm); it designates the JSON-LD @type / rdf:type. Consumers MUST tolerate unknown values by treating the concept as a generic lokf:Concept.",
                    "enum": [
                        "Tutorial"
                    ],
                    "type": "string"
                },
                "usage_window": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/UsageWindow"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "The `{ from, to }` date range framing every `usage_count`. Attached both to Concept (the shared window, sibling of `sources`) and to Source (a per-entry override)."
                },
                "verified": {
                    "description": "Verification events confirming the content against its sources or resource. Independent of `generated`: who wrote a concept need not be who confirmed it. Trust tiers derive from the actors: absent = unverified; only non-human actors = machine-confirmed; any `human:<id>` actor = human-reviewed. A bare `{ by, at }` mapping MUST be read as a one-element list; the LOKF loader normalizes the bare form before LinkML validation. Minted in lokf: no established vocabulary has an entity-to-verification-event property.",
                    "items": {
                        "$ref": "#/$defs/Verification"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "version": {
                    "description": "A version string for the concept's content.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "required": [
                "id",
                "type"
            ],
            "title": "Tutorial",
            "type": "object"
        },
        "UsageWindow": {
            "additionalProperties": false,
            "description": "The `{ from, to }` date range framing every `usage_count` signal (OKF v0.2 \u00a75.1). Written once as a sibling of `sources`; a single Source entry MAY carry its own override.",
            "properties": {
                "from": {
                    "description": "Start date (inclusive, YYYY-MM-DD) of a usage window.",
                    "format": "date",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "to": {
                    "description": "End date (inclusive, YYYY-MM-DD) of a usage window.",
                    "format": "date",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "title": "UsageWindow",
            "type": "object"
        },
        "Verification": {
            "additionalProperties": false,
            "description": "One verification event (OKF v0.2 `verified`, \u00a75.2): an actor confirmed the content against its sources or resource at a time. Independent checks accumulate as separate events; trust tiers (unverified / machine-confirmed / human-reviewed) are derived from the actors, never stored. PROV has no Verification class, so the class IRI is honestly lokf-minted; the generated ontology declares it a subclass of prov:Activity.",
            "properties": {
                "at": {
                    "description": "ISO 8601 datetime at which the event completed.",
                    "format": "date-time",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "by": {
                    "description": "The actor that performed the event, as a plain OKF \u00a77 actor string (`<producer>/<version>`, `human:<id>`, `process:<id>`) \u2014 a literal, never an IRI. Trust tiers derive from the `human:` prefix via string inspection (SPARQL STRSTARTS).",
                    "type": "string"
                }
            },
            "required": [
                "by"
            ],
            "title": "Verification",
            "type": "object"
        }
    },
    "$id": "https://w3id.org/lokf/schema",
    "$schema": "https://json-schema.org/draft/2019-09/schema",
    "additionalProperties": true,
    "description": "A self-contained, hierarchical collection of concept documents \u2014 the unit of distribution (a git repo, tarball, or subdirectory). Maps to a DCAT Catalog. When a whole bundle is serialized as a single JSON-LD document, this class is the tree root; individual concept files validate against Concept (or a subclass).",
    "metamodel_version": "1.11.0",
    "properties": {
        "base_iri": {
            "description": "The base IRI against which Concept IDs are resolved to produce each concept's stable `id`.",
            "format": "uri",
            "type": [
                "string",
                "null"
            ]
        },
        "concepts": {
            "description": "The concepts contained in a bundle.",
            "items": {
                "anyOf": [
                    {
                        "$ref": "#/$defs/Dataset"
                    },
                    {
                        "$ref": "#/$defs/Metric"
                    },
                    {
                        "$ref": "#/$defs/Service"
                    },
                    {
                        "$ref": "#/$defs/Playbook"
                    },
                    {
                        "$ref": "#/$defs/Tutorial"
                    },
                    {
                        "$ref": "#/$defs/Explanation"
                    },
                    {
                        "$ref": "#/$defs/Policy"
                    },
                    {
                        "$ref": "#/$defs/GlossaryTerm"
                    },
                    {
                        "$ref": "#/$defs/Reference"
                    },
                    {
                        "$ref": "#/$defs/Document"
                    },
                    {
                        "$ref": "#/$defs/Role"
                    },
                    {
                        "$ref": "#/$defs/AttestedComputation"
                    },
                    {
                        "$ref": "#/$defs/Person"
                    },
                    {
                        "$ref": "#/$defs/Organization"
                    },
                    {
                        "$ref": "#/$defs/Table"
                    }
                ]
            },
            "type": [
                "array",
                "null"
            ]
        },
        "context": {
            "description": "The URL of the JSON-LD @context to attach to this bundle's concepts to interpret their frontmatter as Linked Data.",
            "format": "uri",
            "type": [
                "string",
                "null"
            ]
        },
        "description": {
            "description": "A single-sentence summary of the concept.",
            "type": [
                "string",
                "null"
            ]
        },
        "license": {
            "description": "The license under which the concept or bundle is offered.",
            "type": [
                "string",
                "null"
            ]
        },
        "lokf_version": {
            "description": "The LOKF version the bundle targets (e.g. \"0.1\").",
            "type": [
                "string",
                "null"
            ]
        },
        "okf_version": {
            "description": "The OKF version the bundle remains compatible with (e.g. \"0.1\").",
            "type": [
                "string",
                "null"
            ]
        },
        "publisher": {
            "anyOf": [
                {
                    "$ref": "#/$defs/Person"
                },
                {
                    "$ref": "#/$defs/Organization"
                },
                {
                    "type": "null"
                }
            ],
            "description": "The agent responsible for making the bundle available."
        },
        "title": {
            "description": "Human-readable display name.",
            "type": [
                "string",
                "null"
            ]
        }
    },
    "title": "lokf",
    "type": "object",
    "version": "0.5.0"
}

